# 네이티브 빌드 체크리스트 (현재 대상: **1.0.39** / iOS build53 · Android vc70 — 2026-08 초 양대 동시 제출)

2026-07-19 작성 — 메모리/커밋에 흩어진 대기 항목 통합. 어떤 모델/세션이든 이 문서만으로
다음 릴리스를 진행할 수 있게 하는 것이 목적. 절차 기준: CLAUDE.md 빌드/배포 섹션.

## 0-AAAAA. ★★2026-08-03 iOS 1.0.39(build56) 리젝 — Guideline 5 Legal: CallKit / 중국★★

**심사 결과**: 리젝. Submission ID `e3c17b24-fd63-4564-a50c-cad453dbf0f4`, 심사 기기 iPad Air 11"(M3).

> Recently, the Chinese Ministry of Industry and Information Technology (MIIT) requested that
> CallKit functionality be deactivated in all apps available on the China App Store. …
> This app cannot be approved with CallKit functionality active in China.

**원인**: `modules/focus-shield/ios/FocusShieldModule.swift`의 `import CallKit` + `CXCallObserver`.
2026-08-01에 **iOS 전화 수신 시 이탈 오탐을 고치려고** 넣은 것이다(CLAUDE.md 전화 처리 절).
라이브인 1.0.36에는 없었으므로 **이번 빌드에서 처음 걸린 것이 맞다** — 회귀가 아니라 신규 표면.

### ★관측 전용이라는 사실은 방어가 되지 않는다★
`CXCallObserver`는 통화를 **읽기만** 하고 `CXProvider`/`CXCallController`(통화 UI·VoIP 제공)는
전혀 안 쓴다. 그래도 리젝이다 — 심사는 **바이너리가 CallKit을 링크했는지**를 보지 통화 UI 유무를
보지 않는다. "우린 VoIP 앱이 아니다"로 다투지 말 것.

### 해결책 3안과 선택

| | 내용 | 판단 |
|---|---|---|
| **A. 중국 본토 배포 해제** | ASC → 가격 및 사용 가능 여부 → **China mainland 체크 해제**. 코드·빌드 **무변경** | ★**채택**★ |
| B. CallKit 제거 | 08-01에 고친 iOS 전화 이탈 오탐이 그대로 되살아남. iOS엔 대체 수단이 없다(안드 `AudioManager.getMode()` 대응물 부재) | 대가가 큼 |
| C. 중국 리전에서만 비활성 | 리빌드 필요 + 리전 판정 기준(캐리어 MCC·스토어프론트)이 불확실해 재리젝 위험 | 지역 1개 값으론 과함 |

A인 이유: 한국어 전용 수능·공시 학습앱이라 **중국 본토 App Store의 값어치가 사실상 0**이고,
B는 실기기로 검증까지 마친 기능을 되돌리는 것이다. **홍콩·마카오·대만은 MIIT 대상이 아니므로 유지**한다.

### ★이 제약은 영구적이다★
- `import CallKit`이 코드에 남아 있는 한 **중국 본토를 다시 켜는 순간 재리젝**된다.
  배포 지역을 손댈 일이 생기면 이 절을 먼저 볼 것.
- **build57(1.0.40)도 같은 CallKit을 갖고 있다** — 지역 해제 없이 1.0.40으로 갈아타도 똑같이 리젝된다.
  즉 지역 해제는 **어느 빌드로 재제출하든 선결 조건**이다.

### 해결 센터 회신 초안 (★지역 해제를 실제로 끝낸 뒤 보낼 것★)

```
Thank you for the review.

We have removed China mainland from this app's available territories in App Store Connect,
so CallKit functionality is no longer available to users on the China App Store.

For additional context: this app is a Korean-language study timer and provides no calling or
VoIP feature of any kind. We use CallKit in a read-only manner only — a single CXCallObserver
instance — to detect whether the user is currently on a phone call, so that our focus-session
feature does not incorrectly penalize the user when an incoming call sends the app to the
background. We do not use CXProvider, CXCallController, or any CallKit call UI.

Please let us know if any further change is required.
```

### 처리 순서

★**재제출은 build57(1.0.40)로 간다** — 2026-08-03 사용자 결정.★
1.0.39는 버린다. build57은 이미 ASC 업로드 + TestFlight 실기기 전항목 통과(4.5·4.6·4.8·4.9절)라
재빌드 비용이 0이고, **심사 사이클을 한 번 아낀다**(1.0.39 재심사 → 다시 1.0.40 심사 = 2회 → 1회).
대가는 릴리스 노트를 `1.0.36 → 1.0.40` 범위로 다시 쓰는 것뿐 → **6.5-A2절에 확정본 있음**.

- [ ] ASC → 가격 및 사용 가능 여부 → **중국 본토만** 해제 (홍콩·마카오·대만 유지)
- [ ] 해결 센터에 위 문구로 회신
- [ ] iOS **build57 / 1.0.40** 제출 — 릴리스 노트는 **6.5-A2절 iOS 판**
      (6.5-A절 1.0.39 초안을 그대로 쓰면 음성 메모·백업·시간 입력이 빠진다)
- [ ] App Privacy 답변에 오디오 항목이 새로 필요한지 확인 (마이크가 처음 들어가는 빌드)
- [ ] Android: **vc70(1.0.39) Play 심사 상태 확인** → 승인됐으면 vc72(1.0.40) AAB 빌드·제출
      (노트는 6.5-A2절 Android 판, vc70 미승인이면 범위 재조정)
- [ ] 승인 후 이 절을 이력으로 남기고 0-AAAA절 갱신 + `sdk56` → `main` 머지

## 0-AAAA. ★2026-08-01 종료 시점 — 양대 심사 중, 승인 후 할 일만 남음★

| | 상태 |
|---|---|
| Android vc70 (1.0.39) | Play 심사 중 (전체 출시 100%) + **ASO 스터디 키워드 3필드 동반 제출** |
| iOS build56 (1.0.39) | App Store 심사 중 (설명·노트·스크린샷 갱신 동반) |

**승인 후 순서** (전부 아래 절에 상세):
1. **Play 승인 → OTA** 계획 타이머 결과 모달 5분 통일(`2ecfc55`, 순수 JS) — 0-AA절
2. **Android 승인 → 웹사이트 (A)** 모드 이름·"10초"·"즉시 알림"·화면 끄기 — 6.6절
3. **iOS 승인 → 웹사이트 (B)** 사진첨부 태그 제거·스터디룸 후속 — 6.6절
4. **양대 승인 → `sdk56` → `main` 머지** — 0-A절
5. Play 등록정보 다음 수정 때 **울트라모드 '10초' 문구 제거** — 6.5-B절

**다음 빌드에 실릴 것**(지금 sdk56에만 있음):
- 수능 시간표 라벨에서 학년도 제거(`0d71b88`) — 앱 문구
- 스크린샷 재캡처 3건(아래) 반영

**Play 스크린샷도 2장 겹침으로 개편** (2026-08-01 결정, 미실행):
iOS는 9장 전부 겹침으로 통일했는데 **Play는 05~08 네 장이 아직 단독 폰**이다
(play/01·02·03은 `src_shot1~3`을 공유해 이미 겹침, play/04는 `src_play4`가 이미 겹침).
- 대상: `src_play5.html` ~ `src_play8.html` → `play/05.png` ~ `play/08.png`
- 짝은 iOS와 같은 걸 쓰면 된다(6.6절 아래 표). ★단 **07번은 다르다**★ —
  iOS는 홈 위젯 + **잠금화면 위젯** 조합인데 **안드로이드엔 잠금화면 위젯이 없다.**
  Play용은 홈 위젯 + 다른 화면(예: 위젯 설정)으로 갈 것
- 렌더 방법(iOS와 다름): Play는 1080x2160이라 **배율을 줘야 한다** —
  `--force-device-scale-factor=0.8372 --window-size=1290,2580`.
  1080x2160 창으로 그냥 뽑으면 잘린다(08-01에 한 번 밟았다)
- 우선순위는 낮다. **재캡처 3건을 먼저 처리하고 같은 회차에 함께** 하는 게 효율적이다

**재캡처 대기** — 앱 화면 캡처 안에 박혀 있어 HTML로는 못 고친다:
- `ios_widgets_home5.jpg` — 오늘할일 위젯에 테스트 입력 **'ㄹㄹ'** 노출 ★우선순위 1★
- 스터디룸 캡처 — 좌석 태그가 '울트라집중'(앱은 `MODE_LABEL`로 **'울트라'**)
- `stat-grass.jpg` 등 — 두부 문자(공부 일기/휴식). **앱 버그 아님**(Ionicons를 쓴다) — 캡처 시점 폰트 로딩

## 0-AAA. ★빌드 번호 대장 — 다음 빌드 전에 반드시 확인★

올린 번호는 **재사용 불가**다(ASC/Play가 거부). 빌드 전에 `app.config.js`를 여기와 대조할 것.

| 플랫폼 | 마지막 **업로드된** 번호 | 다음에 쓸 번호 |
|--------|------------------------|---------------|
| iOS `buildNumber` | **57** (1.0.40, 2026-08-03 TestFlight 검증용) | **58** |
| Android `versionCode` | **70** (1.0.39, 2026-08-01 Play 제출) · 72는 1.0.40 코드에 반영(미업로드) | **74** (짝수 관행) |

- ★TestFlight에 **53·54·55가 모두 보인다 — 반드시 55를 쓸 것**★
  - **build53 폐기**: 이탈 판정 수정 전(20초 복귀에도 이탈 1회)
  - **build54**: 이탈 판정은 정상이나 **결과 모달의 '시간 수정'이 안 열린다**(iOS 형제 Modal)
  - **build55**: 시간 수정 수정 + 계획 타이머 결과 모달 5분 통일 (검증 통과)
  - **build56(현재)**: '다른 앱 → 화면 끔'에서 이탈 알림·카운트 누락 수정 (Swift 변경 포함)
- build52 = 1.0.36 / 1.0.37·1.0.38은 iOS 미빌드
- ※빌드 번호 관리는 **어시스턴트 책임**(사용자 위임 2026-08-01)

## 0-AA. ★현재 진행: 1.0.39 — iOS·Android 동시 제출 (2026-08 초)★

- [x] 이전 릴리스: **Android 1.0.38(vc68) 2026-07-29 승인·라이브** (사용자 확인).
      iOS는 1.0.37·1.0.38을 **빌드하지 않음**(무료 빌드 할당량 소진, Windows 로컬 불가)
- [x] 버전 반영: `version 1.0.39` / `android.versionCode 70` / `ios.buildNumber 53`
- [x] 테스트 468개 통과
- **1.0.39가 처음 출시하는 것** (전부 네이티브 필요 — OTA 불가/안 함):
  | 항목 | 절 |
  |------|-----|
  | 화면 끄기·기기 잠금은 이탈이 아님 (안드 screenState / iOS focus-shield) | 1.6 |
  | 이탈 알림 네이티브 이관 `AwayWatch` + 첫 알림 5초 지연 (안드, 07-30 · **실기기 전항목 통과**) | 1.6 |
  | 이탈 인정 기준 10초 → 15초 (`AWAY_MIN_MS` 단일화) | 1.6 |
  | 전화 수신·통화 직후 60초는 이탈 아님 (안드, 07-31 · **실기기 4항목 통과**) | 1.6 |
  | 전화 수신·통화 직후 60초는 이탈 아님 (**iOS, 08-01 신규** — CallKit `CXCallObserver`, 미검증) | 1.6 |
  | 공부 모드 문구 통일 (일반모드/집중모드/울트라모드, 설정탭 '공부 모드') | — |
  | 시작 팝업 '다음부터 묻지 않기'(`modeAutoStart`) + 설정 토글 (07-31 · **실기기 통과**) | 1.8 |
  | 🔥모드 화면 꺼짐을 시스템에 맡김 (keep-awake 해제) | 1.7 |
  | iOS: 상시 타이머 알림 대응물 없음 — iOS는 1.0.36 이후 **모든 sdk56 변경분이 처음** 나가는 빌드 | — |
- ★**iOS는 1.0.36 → 1.0.39 점프**라 스터디룸 라운지 신고·숨기기, 오답노트 사진 첨부,
  결과 모달 분리 등 그동안의 JS 변경이 **한꺼번에 나간다** — 애플 심사(1.2 UGC) 대비 확인할 것★
- [x] **오답노트 편집기 안드 키보드 회귀 확인 — 2026-08-01 실기기 통과**(2026-07-29 수정): 내용 입력 중 맨 아래
      '이 오답 삭제'가 깜박이던 문제 → `KeyboardAvoidingView behavior`를 안드에서 `undefined`로
      (창이 이미 pan으로 밀리는데 height까지 줄여 이중 조정되던 것. FocusScreen이 쓰던 방식과 동일).
      **확인할 것: 키보드를 올린 채 제목/내용/챕터 입력창이 가려지지 않고 스크롤로 닿는지.**
      가려지면 되돌리기는 한 줄(`undefined` → `'height'`)
- [x] **EAS 빌드 (2026-08-01)**: Android vc70 `31a860d2-e25d-499a-84c6-afbbb2b7e983` FINISHED /
      iOS build53 `b72be57d-d102-4174-9715-decd2238c8b9` **FINISHED** (커밋 `f52d417` = iOS 전화 처리 포함)
      - ★**`modules/focus-shield` Swift가 처음으로 컴파일에 성공했다**★ — 07-28의 잠금 감시
        코드와 08-01의 CallKit 코드가 여기서 처음 빌드를 통과했다(Windows라 로컬 검증 불가였음).
        **컴파일이 됐다는 것이지 동작한다는 뜻은 아니다** — 실기기 검증은 그대로 남아 있다
      ※iOS는 무료 할당량이 **8/1 당일에도 바로 리셋되지 않아** 첫 시도가 거부됐다
      (`This account has used its iOS builds from the Free plan this month, which will reset in 1 hour`)
      → 약 1시간 뒤 재시도로 통과. **8/1이 되자마자 되는 게 아니므로 재시도 여유를 둘 것**
- [x] **Play 프로덕션 제출 완료 (2026-08-01)** — vc70(1.0.39) 전체 출시(100%), 검토 중.
      변경 4건 = 프로덕션 릴리스 + 앱 이름 + 간단한 설명 + 전체 설명
- [x] **★스터디 키워드 3필드 제출됨★** — 07-26에 '저장만' 해두고 vc68 승인을 기다리던
      앱 이름/간단한 설명/전체 설명이 이번 제출에 함께 나갔다. **ASO 숙제 닫힘**
      (`project_aso_strategy` 메모리의 '저장만 된 상태' 기록은 이제 낡음)
- [x] 출시 노트: 6.5-A절 **Android 버전**으로 교체 확인 (1.0.38 노트 재사용 방지)
- [x] Play 전체 설명에서 **'iPhone은 잠금화면에도' 문장 제거** — 통합본에 있던 것을 Play용에서
      뺐다. ★Play 리스팅에 다른 플랫폼 기능을 적으면 안드 사용자에겐 쓸 수 없는 안내가 된다★
      (App Store 설명에는 'iPhone' 단어 없이 '잠금화면에도 올릴 수 있어요'로 남긴다)
- [x] AI 애셋 신고: `Label assets as created or edited using AI` (근거·애셋별 기준은 6.5-C절)
- [x] **iOS TestFlight 업로드 완료 (2026-08-01)** — submission `a1da9193-e8d4-40c5-890d-a682f6ed3462`,
      ASC 처리 대기. ※`eas build`는 IPA만 만든다 — **TestFlight에 올리려면 `eas submit`이 따로 필요**
      (`eas submit --platform ios --id <build> --profile production`, ASC API Key는 EAS에 설정돼 있어 무프롬프트)
- [x] ★**iOS 실기기 검증 — 핵심 전부 통과 (2026-08-01, build54)**★
      1.6절 잠금 감지(10분 초과까지 0회) · 판정 3구간(20초 복귀 0회 / 30초 이상 1회 / 1분 챌린지) ·
      1.7절 자동 잠김 · 슬라이드 잠금해제 회귀 · **계정 영속 재설치 uid 유지**(첫 검증) ·
      **스터디룸 신고·숨기기**(애플 1.2 UGC 대비 확인) · **울트라모드 앱 차단(Screen Time)** ·
      오답노트(사진 첨부 포함) · 결과 모달
      - ⚠**build54에서 발견돼 고친 것**: 결과 모달의 '시간 수정'이 **iOS에서만** 안 열렸다
        (형제 Modal → 오버레이, 커밋 `375a1bf`)
- [x] ★**build55 검증 통과 (2026-08-01) — iOS 실기기 검증 종료**★
      - 계획 타이머를 5분만 돌려도 결과 모달이 뜬다(80% 게이트 제거, `2ecfc55`)
      - 그 모달에서 **시간 수정이 열린다** — iOS 형제 Modal 수정 + 계획 세션이 `sessionId`로
        오게 된 두 변경이 겹치는 지점이라 여기서 함께 확인됐다
      - 80% 초과 세션만 '계획 달성!', 미만은 '공부 완료!'
      - 회귀: 일반 타이머 모달·시간 수정 그대로
      → **iOS는 심사 제출 준비 완료.** 남은 선택 항목(넛지 90초·FaceTime·위젯)은 출시를 막지 않는다
- [ ] 남은 검증(선택): 넛지 90초 / 잠금·해제 반복 시 강제 종료 / 울트라모드 잠금 시 타이머 유지 /
      '다음부터 묻지 않기' iOS / FaceTime 통화 3종 / 위젯 5종
- [ ] ~~iOS 실기기 검증 — 전항목 미실시~~: 1.6절(화면 끄기·잠금 / **전화 7차**) + 1.7절.
      ★우선순위 1: **2분 통화 → 끊고 복귀 → 이탈 0회**★ — `consumeCallHeld` 분기는 iOS에서만
      도는 경로라 이 시나리오에서만 드러난다(백그라운드 태스크 만료 이후)
- [ ] **build56 실기기 검증** (2026-08-01 업로드):
      - [ ] 다른 앱으로 나가 **1분 이상** 있다가 화면 끄기 → 알림이 오고 복귀 시 **이탈 1회** ← 이번 수정
      - [ ] ★**회귀 1순위**: 앱 만지고 **곧바로** 화면 끄기 → 잠금화면에 알림 없음 + 이탈 0회★
            (취소 창을 좁힌 부작용이 있다면 여기서 드러난다)
      - [ ] 회귀: 화면 끄고 10분 방치 → 이탈 0회 / 다른 앱 30초 이상 → 이탈 1회
- [x] ★**iOS App Store 심사 제출 완료 (2026-08-01)**★ — build56, 설명·프로모션 텍스트·
      릴리스 노트·스크린샷 9장 함께 제출. **양대 스토어 모두 심사 중 → 승인 대기가 다음 관문**
      - 설명에 **오답노트 섹션을 신설**했다 — 1.0.36부터 있던 기능인데 iOS 설명에 한 줄도
        없었다(Play엔 있었다). 섹션 번호를 밀어 11개로 재정렬
      - 울트라모드 설명에 **iOS만의 스크린 타임 앱 차단**을 넣고, 9번에 **잠금화면 실시간
        타이머(Live Activity)** 한 줄 추가 — 둘 다 iOS 강점인데 안 적혀 있었다
- [ ] ~~iOS App Store 심사 제출~~ — **build56**을 고를 것(53·54·55 아님).
      - 설명 갱신을 **같은 제출에 함께**(6.5-B절) — 스터디룸 후속·사진 첨부는 승인돼야 실재한다
      - 설명 3번 울트라모드는 **숫자 없이** '잠깐만 다른 앱을 써도' (Play엔 '10초'가 이미 나가서
        다음 등록정보 수정 때 고친다 — 6.5-B절 숙제)
      - 1.2 UGC: 라운지 신고·숨기기가 iOS 첫 출시 (동작 확인됨 2026-08-01)
- [ ] **Play vc70 승인 후 OTA** — 계획 타이머 결과 모달 5분 통일(`2ecfc55`)은 **순수 JS**라
      `eas update --channel production`으로 나간다(runtimeVersion=appVersion=1.0.39).
      ★그래서 안드를 재빌드하지 않았다★ — 재빌드하면 심사가 리셋되고 **07-26부터 기다린
      스터디 키워드 3필드 제출까지 함께 지연**된다(2026-08-01 판단).
      ※OTA는 **승인 후에** 쏜다 — 심사 중에 쏘면 심사원이 제출본과 다른 코드를 본다

## 0-A. 직전 릴리스: 1.0.38 (Android vc68) — 2026-07-26 제출 → **07-29 승인·라이브**

- [x] 이전 릴리스 상태: **Android vc64(1.0.37) 승인·배포 완료** (사용자 확인, 07-26).
      iOS는 1.0.37을 **빌드하지 않음** (무료 빌드 할당량 소진) → iOS는 8/1 이후 1.0.38로 직행
- [x] 버전 반영: `version 1.0.38` / `android.versionCode 68` / `ios.buildNumber 53`(iOS 미빌드 상태 유지)
- [x] 테스트 441개 통과
- [x] **EAS 빌드**: vc68 `ebc69b53-5efe-464d-be77-332b403dc61d` (커밋 318f5a3) — FINISHED.
      ※**vc66은 미제출 폐기** (`679ed0ab`, 커밋 72201d5) — 아래 '기기 지원 중단' 때문
- [x] **Play 프로덕션 제출 완료 (2026-07-26)** — 전체 출시(100%), 심사 중
- [x] 릴리스 노트: "재설치해도 유지" 과대약속 블록 **삭제하고 새로 작성** (6.5절 숙제 소화됨)

### ★이번에 밟은 함정: CAMERA 권한이 Play 배포 대상 기기를 줄인다★
vc66 검토 화면에 **"이전 출시 버전에서 지원되던 기기 402개를 더 이상 지원하지 않습니다"**
(전화 68 -1%, 태블릿 263 -4%). 원인은 오답노트 사진 첨부로 들어온 `android.permission.CAMERA` —
안드로이드가 이 권한을 보고 `android.hardware.camera`(+`camera.autofocus`)를 **필수 하드웨어로 암시**해
카메라·오토포커스 없는 기기(특히 저가 태블릿)를 제외한다. 권한은 expo-image-picker 라이브러리
매니페스트에도 있어 제거로는 못 푼다.
→ **해결: `plugins/withCameraNotRequired.js`** (uses-feature required="false" 2개). 커밋 318f5a3.
→ 결과: vc68에서 **402대 → 16대(~0%)** 로 회복, 그대로 제출.
**앞으로 권한을 새로 추가할 때마다 검토 화면의 '지원 기기 변경사항'을 반드시 확인할 것.**
(참고: '가독화 파일 없음' 경고는 무해 — `enableMinifyInReleaseBuilds` 기본 false라 매핑 파일이 없다.)
- **이번 빌드로만 활성화되는 네이티브 변경** (vc64 이후):
  | 항목 | 커밋/모듈 | 상태 |
  |------|-----------|------|
  | 안드 상시 타이머 알림 (chronometer) | `modules/timer-notif` (4번째 로컬 모듈) | 실기기 확인 완료(07-25) |
  | 오답노트 사진 첨부 | expo-image-picker / expo-image-manipulator + CAMERA 권한 | 실기기 확인 필요 |
  | iOS 오늘할일 위젯 stale done 필터 | `targets/widgets/TodayTodoWidget.swift` | iOS 빌드 대기(8/1) |
- **JS로 함께 실리는 대형 변경** (OTA로도 갈 수 있으나 이 빌드에 포함): 스터디룸 대개편 C/A/B/H,
  익명 응원, 방 리스트, 프로필 수정, 결과 모달 App.js 루트 분리
- 미검증 잔여: 결과 모달 실기기(07-25 구현), 스터디룸 pill 방이동 갱신, 사진 첨부 실기기

### vc68 승인 후 할 일 (**07-29 승인됨 — 아래가 지금 열린 숙제**)
- [ ] 실기기 검증: 오답노트 사진 첨부(촬영/앨범/5장 상한/삭제), 상시 타이머 알림(설정 토글 끔 포함),
      스터디룸 신고·숨기기, 결과 모달
- [ ] **sdk56 → main 머지·푸시** — ★**1.0.39 양대 승인 후로 연기 결정(2026-07-29)**★.
      vc68 승인으로 원래 조건은 풀렸지만, 지금 sdk56엔 **실기기 검증 전인 1.0.39 네이티브 의존 코드**
      (화면 끄기 이탈 판정·keep-awake 제거)가 있어 머지하면 `main = 출시된 것` 규칙이 깨진다.
      머지가 HTML 갈라짐을 막아주지도 않는다 — 지금은 main ⊂ sdk56(갈라진 것 없음)이고,
      앞으로 main에 웹사이트 커밋이 생기면 머지 여부와 무관하게 또 갈라진다(그땐 역방향 머지 반복).
      아래 원래 메모는 이력으로 보존:
      **2026-07-26에 준비 완료**: 보류 중 웹사이트 작업이 main에서 직접 진행돼(11커밋) 같은 HTML이
      양쪽으로 갈라져 있었고, 방치하면 머지 때 main의 SEO 수정이나 개인정보처리방침 한쪽이 조용히
      날아갈 상태였다. `main → sdk56` 역방향 머지로 충돌 3건을 선해소(ab3cca9) →
      지금 main은 sdk56의 완전한 조상(뒤짐 0)이고 **머지 충돌 0건**. 승인 후 그냥 머지하면 됨.
      ※교훈: 웹사이트는 main에서, 앱은 sdk56에서 고치는 구조라 **머지를 오래 미루면 HTML이 갈라진다**.
      개인정보처리방침은 스터디룸(2항)·카메라사진(7항)이 **둘 다** 남아야 정상
- [ ] iOS 빌드 (8/1 무료 할당량 리셋 후, buildNumber 53) — **버전은 1.0.39로 통합**(0-AA절).
      iOS는 스터디룸 신고·숨기기가 아직 미출시라 애플 1.2 UGC 리스크가 그때까지 남아 있음

## 0. 선행 조건 (1.0.36 당시 기록 — 이력 보존)

- [x] Android 1.0.35(vc58) 심사 승인 확인 — **2026-07-20 승인 완료 (양대 배포 끝)**
- [ ] sdk56 → main 머지·푸시 — **1.0.36 승인 후로 보류** (1.0.35 이후 sdk56에 스터디룸·계정영속
      미출시 커밋 대량, 이번 1.0.36 빌드로 함께 출시). 승인 후 머지 예정
- [x] 버전 결정: **1.0.36 / iOS buildNumber 51 / Android versionCode 60** — app.config.js 반영 완료 (2026-07-20)
      ※OTA 반영이 느린 문제(사용자 피드백 07-20)로, 스터디룸 등 대형 기능은 OTA 대신 네이티브 빌드로 출시
- [x] **EAS 빌드 큐 완료 (2026-07-20)**: iOS testflight `4dc97d1d-0b8c-4f0a-8cd5-cfd0b4d481be` /
      Android production AAB `e0ae50a6-7812-4c3e-af59-8f18a23276e3`. 테스트 396개 통과.
      다음: 빌드 완료 → 실기기 검증(2절 계정영속 필수) → 스토어 제출 → 승인 후 sdk56→main 머지

## 1. 이 빌드에 새로 실리는 네이티브 변경 (전부 코드 반영 완료, 빌드로만 활성화)

| 항목 | 커밋 | 검증 방법 |
|------|------|----------|
| 계정 영속: iOS 키체인 인증 (durableAuthStorage + expo-secure-store) | f854bca | 아래 2절 시나리오 |
| 계정 영속: Android allowBackup true | f854bca | 아래 2절 시나리오 |
| iOS 위젯 stale 스냅샷의 어제 완료 항목 제거 (SharedData.swift) | 이 커밋 | 앱 안 연 다음날 아침 오늘할일 위젯에 어제 완료가 없어야 |
| expo-store-review 네이티브 리뷰 시트 (현재는 스토어 URL 폴백) | b3b7b61(07-15) | 세션 5회+ 후 리뷰 요청이 인앱 시트로 뜨는지 |
| 스터디룸 클립보드 초대 코드 감지 (expo-clipboard, 구빌드는 조용히 꺼짐) | 07-19 심야 | 초대 문자를 복사 → 스터디룸 로비 진입 → "초대 코드 발견" 입장 제안 확인 |

## 1.5 ★다음 Android 네이티브 빌드(vc66+) 대기 — 상시 타이머 알림 (2026-07-25 구현)

사용자 피드백 대응: 일반모드에서도 앱 밖(상단바)·잠금화면에서 흐르는 공부시간 확인.
iOS는 Live Activity로 이미 되는 것의 안드 대응물.

- 코드: `modules/timer-notif`(Kotlin chronometer 상시 알림) + `src/utils/ongoingNotif.js`(sig 비교 동기화)
  + useAppState 연결(LA 동기화 effect·bg/fg 핸들러) + 설정 토글 `timerOngoingNotif`(알림 세부 설정, 안드 전용)
- **vc64(1.0.37)에는 미포함** — 네이티브 모듈이라 OTA 불가. JS는 모듈 없으면 no-op이라
  이 커밋을 구빌드에 OTA로 내보내도 안전 (requireNativeModule 실패 → 조용히 꺼짐)
- 실기기 검증 항목: 카운트다운(남은시간 카운트다운)/자유(경과 카운트업)/뽀모·연속(페이즈별) 표시,
  잠금화면 노출, 일시정지 시 정적 표시, 종료·삭제 시 알림 제거, 백그라운드 연속모드 전체남은시간 전환,
  설정 토글 끔 → 즉시 제거, 앱 강제종료 후에도 시간 계속 정확한지(벽시계 앵커)

## 1.6 ★다음 네이티브 빌드 대기 — 화면 끄기/잠금은 이탈 아님 (2026-07-28 구현, 양 플랫폼)

사용자 피드백 대응: 울트라집중에서 **화면 고정을 수락하면** 화면을 꺼도 이탈이 아닌데,
**고정을 거부하면** 화면 끄기가 곧 이탈(넛지 알림 + 이탈 카운트, exam은 타이머 정지)이라 비대칭이었다.
화면 끄기는 다른 앱을 쓰는 게 아니므로 이탈로 치지 않는다. iOS는 앱 고정 자체가 없어 전 사용자가 해당.

- 공통: `src/utils/focusAway.js`(순수 판정, 테스트 14개) + useAppState AppState 핸들러
- **안드(vc70+)**: `modules/screen-pin`에 `screenState()` 추가(PowerManager.isInteractive +
  SCREEN_ON/OFF 브로드캐스트 시각 기록) → background 진입 시 즉시 판정해 이탈 처리 skip
- **iOS(빌드 53+)**: 잠금과 앱 전환이 앱 입장에서 같은 이벤트 + 백그라운드에선 JS 타이머가 멈춤
  → `modules/focus-shield`에 `setLockWatch`/`consumeLockedAt` 추가. background 진입 시
  `beginBackgroundTask`로 25초 버티며 `isProtectedDataAvailable`을 0.5초 간격 감시(잠기면 약 10초 뒤 false).
  JS는 이탈 알림을 **20초 뒤로 예약**(id `away-now`)하고 넛지도 예약만 해두며,
  잠금이 감지되면 **네이티브가 그 예약들을 취소**한다(`AWAY_NOTIF_IDS` ↔ JS identifier 일치 필수).
  이탈 카운트는 복귀 시 확정. **iOS는 이탈 중 LA '이탈 중' 부제를 더 이상 세우지 않음**(의도된 변경)
- **구빌드에 OTA로 내보내도 안전** — 네이티브 함수가 없으면 두 경로 모두 기존(이탈 처리) 동작 유지.
  즉 vc68/빌드52 이하에서는 아무 변화 없음
- 우회 방지(안드): **잠금을 푼 뒤** AWAY_MIN_MS(15초) 넘게 앱으로 안 돌아오면 그 구간은 이탈로 계산
  (잠금화면 알림으로 곧장 다른 앱을 여는 경로)
- ★**2026-07-30 실기기 버그 수정 — 기준점을 화면 켬 → 잠금해제로 교체**★
  - **증상**: 🔥집중 모드 중 화면을 끄고 1분쯤 뒤 켜면 **이탈 1회가 조용히 찍힘**.
    이탈 알림도 챌린지도 없고, 같은 세션의 두 번째부터는 재현이 안 돼 간헐적으로 보였다
  - **원인**: `screenOffAwayMs`가 `ACTION_SCREEN_ON`(전원 버튼 누른 순간)부터 쟀다. 그래서
    **잠금화면에 머문 시간 + 패턴/PIN 입력 시간이 전부 이탈로 계산**됐다. 화면 꺼짐 감지 자체는
    정상이라 `markAway`를 안 거치고 복귀 시 재계산(651~655행)에서만 이탈이 확정 → **알림이 없다**.
    `awayMs`가 10~20초라 60초 기준인 챌린지도 안 뜬다
  - **왜 '오래 꺼야 재현'되는 것처럼 보였나**: 기기의 *화면이 꺼진 후 잠금 시간* 설정 때문.
    짧게 끄면 잠금이 안 걸려 스와이프로 즉시 복귀(이탈 0), 오래 끄면 패턴을 요구받아 복귀가
    10초를 넘는다. **임계는 앱 코드가 아니라 이 OS 설정에 있었다** — 끔 시간과의 상관은 허상
  - **재현 조건(확정)**: 화면 **15초만** 끄고 → 켠 뒤 **잠금화면에서 12초 대기** → 해제 → 이탈 1회
  - **수정**: `KeyguardManager` 직접 조회(`keyguardLocked`/`deviceSecure`)를 `screenState()`에 추가하고,
    `screenOffAwayMs`의 판정을 ①잠김→이탈 0 ②해제시각 기준 ③보안잠금인데 기록 없음→이탈 0
    ④잠금 미설정→화면 켬 기준 순으로 재구성. 구빌드는 필드가 없어 ④로 폴백
  - ★**첫 수정 시도는 실패했다 — 되돌리지 말 것**★: `ACTION_USER_PRESENT`(잠금해제 시각)만
    기준점으로 바꿨더니 증상이 그대로였다. **패턴 해제가 시작되면 액티비티가 먼저 살아나고**
    USER_PRESENT 브로드캐스트와 `isKeyguardLocked` 해제는 그 뒤에 확정되기 때문.
    실기기 진단값: 복귀 시점 `lastUnlockAt`='없음', `keyguardLocked`=true, 화면 켬 24.3초 전.
    **브로드캐스트 타임스탬프는 복귀 순간에 신뢰할 수 없다** — 직접 조회만이 답
  - **실기기 확인 완료(2026-07-30)**: ① 잠금화면 33.7초 체류 → `①잠금 유지 → 면제`, 이탈 0회 ✔
    ② 잠금 풀고 다른 앱 사용 → `②해제 시각 기준`, 이탈 1회 + 챌린지(2분 20초) 정상 ✔
  - 같이 고침: `markAway`의 즉시 알림이 identifier 없이 발송돼 복귀 시 트레이에서 못 내리던 문제
    (`AWAY_NOW_ID` 부여 + `dismissNotificationAsync` 추가)
  - ※**네이티브 변경이라 새 빌드 필요** — OTA로는 안 나간다(구빌드는 폴백으로 기존 동작 유지)
- ★**2026-07-30 2차 제보 수정 — 이탈 시작 시각을 배경에서 직접 관측 + 첫 알림 지연**★
  - **증상 ①**: 화면을 끄고 조금 뒤 **잠금화면 알림창에서 곧장 다른 앱으로** 들어가면
    **첫 번째는 이탈 알림도 이탈 카운트도 없다**(두 번째부터는 정상). 그 경로에서 앱은
    `active`를 한 번도 못 받으므로 복귀 시 역산에만 의존하는데, **화면을 짧게 꺼서 키가드가
    아예 안 걸리면 `ACTION_USER_PRESENT`가 발생하지 않아** `lastUnlockAt`이 비고 →
    규칙 ③(보안잠금인데 해제 기록 없음 = 방금 푼 것)에 걸려 **이탈 전체가 면제**됐다.
    ★즉 ③은 '방금 푼 것'과 '애초에 잠기지 않은 것'을 구분하지 못한다★
  - **증상 ②**: 상단 알림창에서(타이머 상시 알림 등) **앱으로 들어가는 순간 이탈 알림만 오고
    이탈은 안 잡힘**. `markAway`가 background 진입 즉시 알림을 쐈기 때문 — 이탈 여부는 10초
    (`SCREEN_ON_GRACE_MS`)가 지나야 정해지는데 알림이 판정보다 먼저 나가서, 알림창을 내렸다
    올리거나 알림을 눌러 돌아오는 **몇 초짜리 배경 전환에도 알림만 울렸다**
  - **수정 ①(카운트)**: `keyguardLocked`/`deviceSecure`(34a6c28)가 **이 빌드에서 처음 실렸고**,
    규칙 ①②가 실제로 돌기 시작하면서 카운트가 정확해졌다.
    **실기기 확인 완료(2026-07-30)**: 알림창→다른 앱 1분 이상 → 복귀 시 **이탈 문구 입력창(챌린지) 정상** ✔
  - **수정 ②(알림)**: 첫 이탈 알림을 **12초 뒤 예약**(`ANDROID_AWAY_NOTIF_DELAY_SEC`, iOS 20초와 같은 방식)
    으로 바꿔 판정 기준(10초)보다 뒤에 오게 했다. sticky 상태 알림도 같은 시점으로 미룸.
    **실기기 확인 완료**: 다른 앱 전환 시 12초쯤 알림 + 이탈 1회 / 5초 복귀는 알림도 카운트도 없음 ✔
  - ★**시도했다가 걷어낸 것 — 배경 JS 폴링(다시 하지 말 것)**★: "배경에서 2초마다 `screenState()`를
    조회한다"를 구현해 빌드까지 했는데 **한 번도 돌지 않았다**. RN 안드로이드는 액티비티 onPause에서
    JS 타이머를 통째로 멈춘다(`JavaTimerManager.onHostPause` → isPaused + clearFrameCallback,
    `TimerFrameCallback.doFrame`은 isPaused면 즉시 return). **setInterval/setTimeout은 백그라운드에서
    발화하지 않는다.** 코드베이스에 "안드는 백그라운드에서도 JS가 돈다"는 주석이 있었던 게 오해의 출처
- ★**2026-07-30 3차 — 이탈 알림을 네이티브로 이관(`AwayWatch`) + 첫 알림 12초 → 5초**★
  - **왜**: 위 2차 실기기 확인에서 "화면 끄기 → 잠금화면 알림창 → 다른 앱" 경로는
    **카운트·챌린지는 정상인데 이탈 알림만 안 왔다**. 그 구간엔 앱이 `active`를 한 번도 못 받고
    JS 타이머도 멈춰 있어 JS로는 손댈 수 없다. **동적 BroadcastReceiver와 AlarmManager는 배경에서도
    도므로** 그 둘로 같은 판단을 네이티브가 한다
  - **구현**: `modules/screen-pin/…/AwayWatch.kt` (신규) + `AlarmReceiver` 액션 2개 추가 +
    모듈 함수 `armAwayWatch(graceMs, limitAtMs, steps)` / `disarmAwayWatch()`.
    흐름: JS가 화면 끄기 배경 진입 시 무장 → SCREEN_ON/USER_PRESENT마다 `isKeyguardLocked`
    **직접 조회** → 잠금이 풀려 있으면 10초 뒤 확인 알람(다시 꺼지면 취소) → 확인 시점에도
    켜짐+해제면 이탈 확정 → 알림 게시 + 30초/1분/3분/5분 넛지 알람 → 복귀 시 JS가 해제
  - **알림 문구 단일 출처**: useAppState의 `awayFirstNotif`/`awayNudgeSteps` — JS 예약 경로와
    네이티브 경로가 같은 배열을 쓴다. 한쪽만 고치면 경로에 따라 다른 말이 나온다
  - **카운트는 그대로 JS**가 복귀 시 역산으로 확정한다(네이티브로 옮기지 않음 — 검증된 경로 유지)
  - **첫 알림 지연 12초 → 5초**(사용자 결정): 순간 전환(1~2초)만 걸러내는 최소값.
    5~10초 사이 복귀 시 '알림은 왔는데 이탈은 안 세는' 경우가 남는 걸 즉시성을 위해 감수.
    **0~2초로 내리면 2차 증상 ②가 재발**하므로 내리지 말 것
  - ※`limitAtMs`(카운트다운 종료 시각)를 넘겨 그 뒤 알림은 게시하지 않는다 — 타이머가 끝났는데
    "타이머가 돌아가고 있어~"가 뜨는 걸 막는 JS 쪽 가드(`limitSec`)와 같은 규칙
  - ※프로세스가 죽으면 네이티브 armed 플래그도 사라져 남은 알람은 아무 일도 안 한다.
    다만 이미 뜬 알림은 트레이에 남으므로 **콜드스타트 시에도 `disarmAwayWatch()`를 한 번 호출**한다
  - ★**첫 실기기 테스트에서 "첫 시도만 알림이 안 옴"(3번 중 1번꼴)이 나와 즉시 재설계**★:
    잠금 해제를 `ACTION_USER_PRESENT`로 알아냈는데, **잠금화면 알림을 눌러 다른 앱을 여는
    경로에서는 이 브로드캐스트가 오지 않는 경우가 있다.** 그러면 확인 알람이 아예 예약되지 않아
    알림이 통째로 안 나간다. → **화면이 켜져 있는 동안 2초 간격으로 폴링하며 키가드를 직접 조회**
    하도록 바꿨다(꺼져 있으면 폴링 안 함 = 대기 배터리 부담 없음, 확정되면 중단, 상한 200회).
    ※이 파일 계열에서 **"이 판단은 브로드캐스트에 의존하면 안 된다"가 세 번째로 확인된 것** —
    34a6c28(복귀 시 역산), 55e97df(USER_PRESENT 부재), 그리고 이번. 다음에 또 이벤트로 짜지 말 것
  - 네이티브 유예도 **JS 예약과 같은 5초**(`ANDROID_AWAY_NOTIF_DELAY_SEC`)로 맞췄다 —
    어느 경로로 나가든 이탈 알림 체감이 같아야 한다
- ★알려진 한계 (2026-07-28 Fable 교차 리뷰에서 확인 · 전부 수용하고 문서화):
  - **iOS ①**: 기기 암호 미설정 사용자는 감지 불가 → 기존대로 이탈 처리
  - **iOS ② (가장 큼)**: 잠금이 한 번 감지되면 **그 백그라운드 구간 전체가 이탈 면제**된다.
    잠금 → 잠금화면 알림/카메라로 다른 앱을 열어 20분 사용 → 복귀해도 exitCount 0, exam 자동
    일시정지도 안 걸린다. 안드는 `lastOnAt`으로 막지만 iOS는 **잠금 해제 시각을 알 방법이 없다**
    (앱이 정지돼 `protectedDataDidBecomeAvailable`을 못 받음). 공개 API로는 해결 불가 —
    exam 강도의 무결성이 iOS에서 안드보다 약하다는 점을 인지할 것
  - **안드 ①**: SCREEN_OFF 신호가 AppState보다 1.5초 넘게 늦는 기기에서는 이탈 알림/넛지가
    이미 발송된 뒤 복귀 시점에야 되돌려진다(카운터는 정확, 알림은 이미 나감).
    → **2026-07-30 완화됨**: 첫 알림을 12초 뒤로 예약하게 바뀌어, 복귀가 그 전이면 예약이
    취소되므로 알림이 아예 안 나간다. 12초를 넘겨 되돌려지는 경우만 남음
  - **안드 ②**: 복귀 직전 전원 버튼을 짧게 껐다 켜면 `lastOnAt`이 갱신돼 그 이전의 다른 앱
    사용 구간이 면제된다. 막으려면 백그라운드 동안의 화면 켜짐 누적 시간을 네이티브가
    집계해야 함 — 고의 우회 경로라 현재는 미구현
- 실기기 검증 항목 (안드) — **2026-07-28 로컬 릴리스 APK로 핵심 2건 확인 완료**:
  - [x] 🔥모드(고정 거부) 실행 중 전원 버튼 → 화면 끔 → 30초 뒤 켜고 잠금해제 → **이탈 0회 유지,
        복귀 알림/넛지 없음, exam 강도에서도 타이머 계속 실행** ✔ 확인됨
  - [x] 화면 켠 뒤 잠금화면에서 다른 앱 열고 1분 사용 → 복귀 시 **이탈 1회 기록** ✔ 확인됨
        (1분 30초 이탈 → 순차 넛지 알림 정상 → 복귀 시 이탈 문구 입력(챌린지) 표시 → 이탈 1회)
        ※이 항목이 통과해야 "화면만 끄면 전부 면제"가 아니라 **화면을 다시 켠 시점부터
        재계산하는 우회 방지**가 실제로 도는 것 — 위 항목과 반드시 같이 봐야 한다
  - [x] **(07-30 수정분 재검증)** 화면 끔 → 켠 뒤 **잠금화면에서 33.7초 대기** → 패턴 해제
        → **이탈 0회** ✔ 확인됨 (수정 전에는 1회가 찍혔음 — 이번 수정의 핵심 회귀 테스트)
  - [x] **(07-30)** 잠금 푼 뒤 다른 앱 사용 → 복귀 → **이탈 1회 + 챌린지(2분 20초)** ✔ 확인됨
        (우회 방지가 안 죽었음 — ①만 통과하고 이게 0회면 "화면만 끄면 전부 면제"로 뚫린 것)
  - [x] **(07-30 2차)** 화면 끔 → 조금 뒤 켜서 **잠금화면 알림창에서 다른 앱으로 이동** →
        1분 이상 사용 → 복귀 → **이탈 1회 + 챌린지** ✔ 확인됨 (이탈 알림은 3차에서 추가)
  - [x] **(07-30 2차)** 다른 앱으로 전환 → **12초쯤 이탈 알림 + 이탈 1회** / **5초 만에 복귀하면
        알림도 카운트도 없음** ✔ 확인됨 (알림과 판정이 어긋나지 않게 된 것)
  - [x] **(07-30 2차 · 회귀)** 잠금화면 체류 후 복귀 → 이탈 0회 유지 ✔ 확인됨
  - [x] **(07-30 3차)** 화면 끄고 아무것도 안 하다 그대로 복귀 → 알림 0건, 이탈 0회 ✔
  - [x] **(07-30 3차)** 다른 앱 전환 첫 알림 5초 / 3초 복귀는 알림 없음 ✔
  - [x] **(07-30 3차)** 집중모드에서 타이머 진행 알림을 눌러 복귀 → 이탈 알림 없음 ✔
        (2차에서 고친 '열리는 순간 알림만 옴' 증상이 실제로 사라진 것)
  - [x] **(07-30 4차 · 폴링판)** 화면 끔 → 켜서 **잠금화면 알림창에서 다른 앱** →
        **첫 시도부터 5초쯤 뒤 이탈 알림** + 후속 넛지 ✔ 확인됨 (3차의 첫 시도 누락이 해소됨)
  - [x] **(07-30 4차 · 회귀)** 화면 끄고 잠금화면에 머물기만 하다 복귀 → **알림 0건, 이탈 0회** ✔
        (폴링이 잠금화면 체류를 이탈로 보지 않음 — 폴링 도입으로 생긴 위험이었음)
  - **→ 1.6절 안드 항목 전부 통과. 남은 미검증은 iOS(8월 EAS 빌드)뿐이다**
- ★**2026-07-30 5차 — 이탈 카운트 기준 10초 → 15초 + 하드코딩 중복 제거**★
  - **왜**: 첫 알림이 5초에 오는데 카운트 기준이 10초면, **알림을 보고 곧바로 돌아온 경우가
    10초 언저리에 걸려 어떨 땐 이탈 1회, 어떨 땐 0회로 갈린다**(사용자 지적). 알림 뒤 10초의
    여유를 둬서 "알림 보고 바로 복귀 = 이탈 아님"이 안정적으로 성립하게 했다 —
    **알림은 벌점 통보가 아니라 돌아올 기회**라는 뜻이 된다
  - ★**이 작업에서 발견: 이탈 기준 10초가 코드에 두 군데 따로 있었다**★
    `focusAway.SCREEN_ON_GRACE_MS`(화면 끄기 경로)와 `useAppState`의 **하드코딩 `awayMs < 10000`**
    (앱 전환 경로). 값이 같아 지금까지 드러나지 않았을 뿐, 한쪽만 고쳤으면 **경로마다 이탈
    기준이 다른** 조용한 버그가 됐다. → `AWAY_MIN_MS` 하나로 합치고 이름도 의미에 맞게 바꿨다.
    **다시 숫자를 박지 말 것**
  - 알림(5초)과 판정(15초)은 **반드시 같이 조정**한다 — 간격이 좁아지면 위 경계 흔들림이 재발
- ★**2026-07-30 6차 — 전화 수신은 이탈이 아님 (안드)**★
  - **왜**: 사용자 질문("집중모드에서 전화가 오면?")으로 드러난 것 — 처리가 **전혀 없었다**.
    통화 화면이 뜨면 배경 전환 → 5초 뒤 '돌아와' 알림 + 30초·1분 넛지가 **통화 중에** 울리고,
    끊고 돌아오면 **이탈 1회 + (1분 넘으면) 챌린지**까지 떴다. 밀도도 깎였다.
    울트라집중만 멀쩡해 보였던 건 **화면 고정이 이 경로를 막고 있어서**이고,
    고정 확인 팝업을 거부한 울트라 사용자는 집중모드와 똑같이 당했다
  - **판별**: `AudioManager.getMode()` — RINGTONE/IN_CALL/IN_COMMUNICATION/CALL_SCREENING.
    ★**`READ_PHONE_STATE` 없이 되는 게 핵심**★ — 그 권한은 Play 정책상 정당화가 까다롭고
    지원 기기까지 줄일 수 있다(vc66에서 CAMERA로 402대 잃은 것과 같은 계열의 함정).
    보이스톡·디스코드 같은 인터넷 통화도 함께 걸린다
  - **적용 지점 3곳**: ①배경 진입(`awayExempt` — 화면 고정과 동일 취급) ②복귀 시점(내려갈 땐
    아직 벨이 안 울렸던 경우를 건짐) ③네이티브 `AwayWatch.onCheck`(잠금화면 위로 걸려온 전화)
  - ※`pinnedNow` → **`awayExempt`로 이름 변경** — 이제 '고정'만이 아니라 '이탈 판정 면제 상황'이다
  - ~~**iOS는 미적용** — 별도 조사 필요(다음 과제)~~ → **2026-08-01 구현됨**, 아래 7차 참조
  - ★**실기기 진단으로 확정된 것 2가지 (2026-07-30)**★ — 추측 두 번 대신 진단을 심어 한 번에 끝냈다
    1. **`AudioManager.getMode()`는 이 기기에서 통화를 실제로 잡는다** — 통화 중 네이티브 폴링에서
       `mode=2`(IN_CALL) 관측. 권한 없는 유일한 수단이라 대안이 없었는데, 유효함이 증명됐다
    2. **통화 구간은 이미 면제되고 있었고, 진짜 문제는 '끊은 뒤'였다** — 실측 로그:
       통화 87초 → `이탈=X`(정상), 그 직후 **20초 구간이 별개 배경 전환으로 잡혀 `이탈=O`**.
       사용자가 말한 "전화 끊고 몇 초 있다가 이탈 알림"이 이것
    - ※1차 진단만으로는 결론을 못 냈다 — 로그의 `mode`가 전부 0이었는데 **통화 중에 찍힌 샘플이
      하나도 없었기** 때문(전화 올 때 앱은 이미 배경이라 JS가 측정할 시점이 없다). "값이 0이다"와
      "0인 값을 못 봤다"를 구분하지 않으면 엉뚱한 결론에 간다. 2차에서 네이티브 폴링이 관측하게 해서 확정
  - **수정**: `POST_CALL_GRACE_MS`(60초) + `awayMsAfterCall()` — 통화 관측 시각 + 유예 이후만
    이탈로 센다. 통째로 면제하면 통화 뒤 다른 앱을 오래 쓰는 우회를 놓치므로 그렇게 하지 않았다.
    `lastCallAt`은 `inCall()`이 true를 돌려줄 때마다 한 곳에서 갱신된다(JS 질의·네이티브 폴링 공용)
  - ★**통화 중에는 화면이 꺼져 있어도 계속 폴링한다**★ (07-30 심야 추가 수정 — 첫 시도 실패 원인)
    유예의 기준점은 '마지막으로 통화를 **관측한** 시각'이다. 그런데 통화 중엔 근접센서로 화면이
    꺼지고, 폴링이 '화면 꺼짐 → 중단'이라 **기준점이 통화 시작 시각에 굳었다** →
    실제 유예가 **`60초 − 통화 시간`**으로 줄어, 30초 통화면 30초만 남고 **2분 통화면 유예가 0**.
    사용자 증상 "20초까지는 괜찮은데 30초쯤에 이탈이 찍힌다"가 정확히 이 계산이다.
    → 통화 중에는 화면 상태와 무관하게 `CALL_POLL_MS`(5초)로 계속 관측해 **통화가 끝난 시각**을
    잡는다. 폴링 상한(checks)도 세지 않는다 — 통화 길이는 사용자가 정하고, 복귀 시 disarm된다
  - ※**"상수를 60초로 넣었다"와 "유예가 60초로 동작한다"는 다르다** — 기준점이 어디서 갱신되는지를
    같이 봐야 한다. 이번 버그가 정확히 그 차이에서 나왔다
  - ★**그 수정도 첫 판은 안 먹혔다 — 고친 자리가 틀렸다**★ (07-31 새벽):
    `onCheck` 안에서 "통화 중이면 계속 폴링"을 넣었는데, **`onScreenEvent`가 SCREEN_OFF를 받고
    `cancelCheck()`로 예약을 지워버려** `onCheck` 자체가 다시 불리지 않았다.
    근접센서로 화면이 꺼지는 순간 폴링이 통째로 끊긴 것. → **두 곳 모두** 통화 중 예외 처리를 넣었다.
    ※교훈: **루프 본문만 고치고 루프를 돌리는 쪽을 안 보면 아무 일도 안 일어난다**
  - 알림 쪽도 같은 유예를 쓴다 — 네이티브가 통화 종료 후 `usableSince`를 **미래 시각**(now+60초)으로
    잡아 그 사이엔 알리지 않는다. ★`POST_CALL_GRACE_MS`가 Kotlin과 JS 양쪽에 있으니 함께 고칠 것★
    (알림은 네이티브, 카운트는 JS가 판단하므로 어긋나면 "알림은 왔는데 이탈은 없음"이 된다)
  - ※`awayMsAfterCall`의 가드는 `callAgoMs >= 0`이 아니라 **타입까지 본다** — `null >= 0`이 참이라
    통화 기록이 없는데 '방금 통화'로 둔갑해 이탈이 통째로 면제되는 버그가 됐다(테스트로 고정)
  - [ ] **(07-30 5차)** 나갔다가 **이탈 알림(5초) 보고 바로 복귀** → **이탈 0회로 매번 일관** /
        20초 이상 나갔다 오면 여전히 이탈 1회 (기준 15초로 올린 효과)
  - [x] **(07-30 6차)** 통화 구간 자체는 이탈로 안 잡힘 ✔ 진단 로그로 확인(87초 → 이탈=X)
  - [x] **(07-31)** 2분 통화 → 끊고 30초 뒤 복귀 → **이탈 0회 + 알림 없음** ✔ 확인됨
  - [x] **(07-31)** 30초 통화 → 끊고 40~50초 뒤 복귀 → **이탈 0회** ✔ 확인됨
  - [x] **(07-31 · 회귀)** 통화 없이 평범한 앱 전환 → **여전히 이탈 1회** ✔ 확인됨
        (통화 유예가 과하게 걸려 이탈 감지를 무력화하지 않았다는 확인)
  - [x] **(07-31 · 우회)** 끊고 2분 이상 다른 앱 사용 후 복귀 → **이탈 1회** ✔ 확인됨
        (통째 면제가 아니라 유예 60초를 뺀 나머지가 잡힌다)
  - **→ 안드 1.6절 전항목 통과. 이 절에서 남은 미검증은 iOS뿐이다** (2026-07-31)

- ★**2026-08-01 7차 — 전화 수신은 이탈이 아님 (iOS)**★ (사용자 요청: "사용자 경험에서 중요")
  - **왜**: 안드에서 고친 것과 **똑같은 증상이 iOS에도 그대로 있었다**. 오히려 iOS는 화면 고정이
    없어 이 경로를 막아주는 것이 아무것도 없다 — 안드에서 '울트라만 멀쩡해 보였던' 착시조차 없다
  - **판별**: CallKit **`CXCallObserver`** (`modules/focus-shield`). ★**권한도 usage description도
    필요 없다**★ — 안드가 `READ_PHONE_STATE`를 피한 것과 같은 이점이고, 심사에 걸릴 요소가 없다.
    셀룰러 통화 + CallKit을 쓰는 인터넷 통화(보이스톡 등)가 모두 잡힌다
  - ★**관측자는 프로퍼티로 붙들 것**★ — 매번 새로 만들면 통화 목록 동기화 전이라 빈 배열이 온다
  - **적용 지점 3곳** (안드와 같은 모양): ①배경 진입(`awayExempt`) ②복귀 시점
    ③잠금 감시 폴링(0.5초) — 통화가 관측되면 **예약된 이탈 알림을 네이티브가 지운다**
  - ★**통화 중에는 `endWatch()`를 부르지 않는다**★ — 안드에서 밟은 '기준점이 통화 시작에 굳는'
    함정과 같은 것. 관측을 계속해야 유예 60초가 실제로 60초로 동작한다
  - ★**iOS 고유 한계 — 통화 '종료 시각'을 못 볼 수 있다**★: 백그라운드 태스크(약 30초)가 회수되면
    관측이 끊긴다. 끊길 때 통화 중이었으면 `consumeCallHeld`로 표시해 **그 배경 구간을 통째로
    면제**한다. 잠금 감지가 이미 하는 양보와 같지만(위 'iOS ②'), **통화 후 다른 앱을 오래 쓰는
    우회는 안드만큼 못 막는다**(수용 — exam 무결성이 iOS에서 더 약하다는 기존 결론의 연장)
  - `POST_CALL_GRACE_MS`·`awayMsAfterCall`은 **JS 공용** — 안드와 같은 60초를 그대로 쓴다
  - ★**셀룰러가 없는 기기에서도 테스트할 수 있다**★ (2026-08-01 — 테스트 기기에 통화 기능이
    없어 막혔던 상황): 코드는 `callObserver.calls`에 통화가 있는지만 보고 **셀룰러인지
    인터넷 통화인지 구분하지 않는다**. CallKit을 쓰는 통화면 전부 같은 경로를 탄다.
    - **FaceTime 오디오**가 가장 간단하다 — 다른 애플 기기에서 테스트 기기로 걸면 된다.
      보이스톡·WhatsApp·Discord도 iOS에선 CallKit을 쓴다
    - ※**FaceTime이 감지되는지 자체가 검증 항목이다** — 안 잡히면 "인터넷 통화도 잡힌다"는
      설명이 iOS에선 사실이 아니라는 뜻이고, 스토어 문구·CLAUDE.md를 고쳐야 한다
  - ★**감지에 실패해도 회귀는 없다(fail-safe)**★ — 통화가 한 번도 관측되지 않으면
    `msSinceCall()`이 -1을 돌려주고 `awayMsAfterCall`의 타입 가드가 그대로 통과시키며,
    `consumeCallHeld()`도 false다. 즉 **수정 전과 완전히 동일하게 동작**한다.
    검증 못 한 채 출시해도 손해는 '이번엔 도움이 안 됐다'까지 — **기능이 안 켜질 뿐이지
    없던 문제를 만들지 않는다**. 그러니 통화 검증이 막히면 **1.6절 화면 끄기·잠금을 먼저**
    할 것(모든 사용자가 매 세션 겪는 경로라 훨씬 중요하다)
  - **실기기 검증 항목 (iOS, 전부 미검증)**:
    - [ ] 🔥모드 중 전화 수신 → **통화 중에 '돌아와' 알림/넛지가 하나도 안 울림**
    - [ ] 30초 통화 → 끊고 20~30초 뒤 복귀 → **이탈 0회**
    - [ ] 2분 통화(백그라운드 태스크 만료 이후) → 끊고 복귀 → **이탈 0회**
          (`consumeCallHeld` 경로 — iOS에서만 도는 분기라 여기서 판가름)
    - [ ] 보이스톡 등 인터넷 통화도 같은지 (CallKit 미사용 앱이면 안 잡힘 — 그때는 한계로 기록)
    - [ ] **FaceTime 오디오로 위 3개 시나리오 대체 수행** (셀룰러 없는 기기일 때 — 위 박스 참조)
    - [x] **회귀**: 통화 없이 평범한 앱 전환 → 20초 뒤 알림 + (30초 이상 체류 시) 이탈 1회
          — 2026-08-01 build54 확인. 통화 코드가 이탈 감지를 무력화하지 않았다는 확인
          (통화 유예가 과하게 걸려 이탈 감지를 무력화하지 않았는지)
  - [ ] 화면 켜고 다른 앱으로 나가기(평소 이탈) → 기존대로 이탈 1회 (미변경 경로, 우선순위 낮음)
  - [ ] 화면 고정 수락 상태에서도 기존과 동일하게 이탈 0 (미변경 경로)
  - [ ] 화면 끄고 몇 분 뒤 복귀 시 밝기/잠금 오버레이가 그대로 유지되는지
- ★**iOS는 "되나 안 되나"가 아니라 "몇 초에 감지되나"를 재는 것이 이번 검증의 목적**★
  잠금 후 보호 데이터가 잠기기까지 걸리는 시간이 기기·직전 잠금해제 시점에 따라 **10~40초로
  들쭉날쭉**하다는 보고가 있고(Apple 개발자 포럼), 백그라운드 태스크 예산은 **약 30초**다.
  즉 느린 쪽에 걸리면 감지 자체가 실패한다 → 그 경우 기존대로 이탈 처리(fail-safe, 손해 없음).
  **잠금 시간을 15초 / 30초 / 1분 / 5분으로 나눠 각각 이탈 0회인지 기록할 것.**
  실패가 잦으면 플랜 B는 `com.apple.springboard.lockcomplete` Darwin 통지(즉시·확실하지만
  비공식 통지명 — 심사 거절·OS 업데이트로 깨질 위험 감수). 2026-07-28 사용자 결정: **먼저 실기기로 판단**
- 참고: Forest(iOS)도 "잠그면 나무가 계속 자람"을 제공하지만, **잠금화면 알림으로 다른 앱을 열면
  그대로 통과되는 같은 구멍**이 있다 — 위 'iOS ②' 한계는 이 카테고리의 표준 수준
- 실기기 검증 항목 (iOS — **암호가 설정된 기기로 테스트할 것**):
  - [x] 🔥모드 실행 중 전원 버튼으로 잠금 → **1분** 뒤 잠금해제 → **이탈 0회, 잠금화면에 이탈 알림/넛지가
        하나도 안 뜸**(20초·30초·1분 지점을 지나므로 네이티브 취소가 되는지 여기서 판가름)
        - ★**2026-08-01 실기기 통과 — 15초·30초·1분·5분은 물론 10분을 넘겨도 이탈 0회**★
        - **이 결과로 '몇 초까지 감지되나' 우려가 해소됐다.** 잠금 감지가 10~40초로 들쭉날쭉하다는
          Apple 포럼 보고 때문에 느린 쪽에 걸리면 실패할 것으로 봤는데, 실측은 10분 이상 안정적이었다
        - → ★**플랜 B(`com.apple.springboard.lockcomplete` Darwin 통지) 불필요**★.
          비공식 통지명이라 심사·OS 업데이트 위험을 감수해야 했는데 안 써도 된다.
          **여기서 종결 — 다시 검토하지 말 것**
  - [ ] 잠금 상태에서 Live Activity 시간이 계속 정확히 흐르는지
  - [x] 다른 앱으로 전환 → **20초 뒤** 이탈 알림(즉시 아님) + 복귀 시 **이탈 1회** — 2026-08-01 통과.
        ★잠금 면제가 과하게 걸려 이탈 감지를 무력화하지 않았다는 확인이라 A항목 중 가장 중요★
  - [ ] **넛지(30초·1분·3분·5분) 미확인** — 위 테스트에서 첫 알림(20초)만 관측됐다.
        예약 목록은 배경 진입 기준 20초/30초/60초/180초/300초이고 **복귀 시 전부 취소**되므로,
        20초 알림을 보고 곧바로 돌아오면 넛지가 안 오는 것이 **정상**이다.
        → 확인하려면 **다른 앱에 90초 이상 머문 뒤** 복귀할 것(20초·30초·60초 3개가 와야 함)
  - [x] 5초만 다른 앱 갔다 복귀 → 이탈 0회 — **build54에서 20초 복귀도 0회로 확인(2026-08-01)**
  - [x] ★**build54 판정 변경 검증 완료 (2026-08-01)**★ — 첫 알림(20초) 보고 **바로 복귀 → 이탈 0회**,
        **30초 이상 → 이탈 1회**, **1분 이상 → 챌린지 문구**. 세 구간이 설계대로 갈린다.
        build53에서 "20초 후 바로 복귀해도 이탈 1회"였던 것이 해소됐고, 챌린지가 iOS 지연 판정
        경로에서도 뜬다는 것까지 확인됐다(코드상으론 공통 블록이지만 실기기 확인은 처음)
  - [ ] exam(울트라모드) 강도에서 잠금 시 타이머가 안 멈추는지
  - [ ] 암호 미설정 기기: 잠금해도 이탈 1회(기존 동작) — 크래시/이상 동작만 없으면 OK
  - [ ] 세션 중 잠금/해제를 반복해도 앱이 강제 종료되지 않는지 (백그라운드 태스크 25초)

## 1.7 ★다음 네이티브 빌드 대기 — 🔥모드 화면 꺼짐을 시스템에 맡김 (2026-07-29 구현, 양 플랫폼)

사용자 요청: 🔥모드가 keep-awake로 화면을 계속 켜 둬서 **배터리 심리적 부담**이 컸다.
"잠금화면 시계를 켜놓고 쓰고 싶어할 사용자는 아주 소수" → **keep-awake를 아예 잡지 않는다.**
무동작 감지·터치 리셋은 전부 OS가 하던 대로 하고, 기기 설정의 화면 시간 초과가 그대로 적용된다.
**1.6(화면 끄기 = 이탈 아님)이 먼저 깔려야 성립**하므로 같은 빌드에 함께 나간다.
**OTA 배포하지 않는다 (2026-07-29 사용자 결정 — 정식 빌드로만).**

- ★**앱이 화면을 직접 끌 수는 없다**★ — 안드는 device admin, iOS는 아예 불가.
  '꺼짐 방지를 안 하는 것'이 전부이므로 **기기가 "화면 계속 켜기"면 안 꺼진다**(문의 시 설명용)
- 설정 항목 없음(항상 동작) — 사용자 결정: "켜기 옵션은 불필요, 자동으로 꺼지면 된다"
- 구현: useAppState `keepScreenAwake()`/`setKeepAwake` + `lastTouchAt`,
  `src/utils/focusAway.js`의 `wasIdleBeforeBackground`(15초, 테스트 3개) +
  App.js **루트 View와 잠금 오버레이**의 `onStartShouldSetResponderCapture`(터치 시각만 기록,
  false 반환이라 터치 처리·슬라이드 해제 PanResponder에 관여하지 않음)
- **안드 구빌드 안전장치**: `screenStateSupported()`가 false면 **예전처럼 keep-awake 유지**.
  안 걸면 vc68 이하에서 화면이 꺼질 때마다 스스로 이탈을 만든다(넛지 알림 + exam 타이머 정지)
- **iOS 판정 근거 = 마지막 터치 시각**: 다른 앱으로 나가려면 반드시 화면을 만져야 하므로
  '15초 넘게 터치 없음 + 백그라운드'는 사람이 나간 게 아니라 화면이 꺼진 것.
  네이티브 잠금 감지(1.6)가 실패하는 **암호 미설정 기기에서도 오탐이 안 나는 것이 이 판정의 목적**
  - 15초로 잡은 근거: **iOS 자동 잠금 최솟값이 30초**라 자동 꺼짐은 항상 마지막 터치 +30초 이후,
    앱 전환은 터치 +1~2초 — 두 경우가 넉넉히 갈린다
  - 부작용: 1.6의 'iOS ②' 한계가 조금 넓어진다 — 방치해서 꺼진 뒤 깨워서 다른 앱을 오래 써도 이탈 0.
    (안드는 `lastOnAt` 기반 AWAY_MIN_MS 규칙이 그대로 적용돼 막힌다)
  - 반대로 **화면을 만지고 나간 정상 이탈은 그대로 잡힌다**(폰을 집어 앱을 전환하면 터치가 남음)
- 문구 변경: 모드 선택 "화면 켜두고 집중 도전!" → **"폰 내려놓고 집중 도전!"**(더 이상 켜지지 않으므로),
  잠금화면 첫 사용 가이드에 "화면이 꺼져도 기록은 계속돼요" 한 줄 추가
- 실기기 검증 항목:
  - [ ] **안드**: 🔥모드 시작 후 방치 → 기기 설정의 화면 시간 초과대로 꺼짐 → 복귀 시 **이탈 0회**,
        넛지 알림 없음 (1.6 검증과 같은 경로)
  - [ ] **안드 exam**: 화면 고정 상태에서도 꺼지고, 켜면 고정된 앱으로 복귀 + 타이머 계속
  - [x] **iOS(암호 설정 기기)**: 방치 → 자동 잠김 → 이탈 알림 안 뜸 → 복귀 시 이탈 0회
        — **2026-08-01 build54 통과**. ★전원 버튼 잠금과 **다른 코드 경로**★(`wasIdleBeforeBackground`
        = 마지막 터치 15초 판정)라 따로 확인이 필요했던 항목이고, 이걸로 1.7절 iOS가 닫혔다
  - [ ] **iOS(암호 미설정 기기)**: 위와 동일하게 이탈 0회여야 함 — **터치 판정의 존재 이유**
  - [ ] **iOS 정상 이탈 회귀**: 화면 만지고 다른 앱으로 전환 → 기존대로 20초 뒤 알림 + 이탈 1회
        (터치 판정이 정상 이탈까지 면제해 버리면 실패)
  - [x] 슬라이드 잠금 해제가 평소처럼 동작(루트/오버레이 터치 capture가 방해하지 않는지) — **2026-08-01 통과**
  - [x] 앱 전반 터치(스크롤·드래그 정렬·모달)가 평소와 동일 — 루트 capture 추가의 영향 없음(2026-08-01)
  - [ ] 기기 화면 시간 초과가 '안 함'이면 안 꺼지지만 앱은 정상(오동작 없음)

## 1.8 시작 팝업 '다음부터 묻지 않기' (2026-07-31 구현 · 실기기 통과)

사용자 요청. 매번 모드를 고르는 게 기본이지만, 늘 같은 모드로 쓰는 사람에겐 팝업이 번거롭다.

- `settings.modeAutoStart`(기본 false). 켜면 팝업 없이 `ultraFocusLevel`로 바로 시작
- 체크는 팝업이 열릴 때마다 **해제 상태로 시작**하고 **실제로 시작한 경우에만** 저장한다
- ★켜면 팝업이 안 뜨므로 **끄는 길은 설정탭 '공부 모드'의 토글 하나뿐**★ →
  첫 자동 시작 때 1회 토스트로 위치를 알린다(`guideAutoStart`). 카드 설명도 상태에 따라 바뀜
- **설정에서 모드를 바꿔도 자동 시작은 유지**한다(암묵 리셋 없음) — 설정에서 모드를 고르는 건
  "이 모드로 하겠다"는 의사표시라, 그걸 자동 시작을 끄는 신호로 보면 사용자가 영문을 모른다
- ※App.js는 `modeAutoStart`면 **오버레이를 렌더 자체를 안 한다** — effect로만 해소하면 한 프레임 깜빡인다

- [x] 팝업 체크박스가 매번 해제 상태 / 체크 후 '취소' → 다시 시작 시 팝업이 다시 뜸 ✔
- [x] 체크 후 시작 → 안내 토스트 → 다음 시작부터 팝업 없이 바로 시작(깜빡임 없음) ✔
- [x] 설정 토글로 되돌리면 다시 팝업이 뜸 ✔
- ※울트라모드 1회 확인 Alert(`guideUltraPick`)은 이미 소진된 기기에서는 재현 불가 —
  '시작하지 않으면 저장 안 됨' 보장은 위 '취소' 항목이 같은 경로를 덮는다

## 2. 계정 영속 실기기 검증 (필수 — docs/account-persistence-design.md 구현 순서 3)

- [x] **iOS**: 설치 → 스터디룸 방 입장 → 앱 삭제 → 재설치 → 같은 uid(닉네임/자리 유지) 확인
      — **2026-08-01 build54 통과.** 키체인 미러가 실제로 동작한다(2026-07-19 구현 이후 첫 검증)
- [ ] iOS: 콘솔에서 SecureStore 2048바이트 초과 경고가 뜨는지 + 그래도 저장되는지
- [ ] **Android**: 설치 → 방 입장 → `adb shell bmgr backupnow com.yeolgong.timer`로 백업 강제
      → 삭제 → 재설치 → uid+공부 데이터 복원 확인 (자동백업은 평소 24h 주기·Wi-Fi 조건)
- [ ] 기존 사용자 마이그레이션: 구버전에서 업그레이드한 기기에서 첫 실행 후 삭제→재설치해도 uid 유지
      (읽기 경로의 키체인 미러가 작동했는지)

## 3. 이전 빌드부터 이월된 검증 항목 (코드는 이미 배포됨)

- [ ] 울트라집중 전체차단(화이트리스트) — 07-05 구현, **실기기 검증 기록 없음** (exam 강도에서
      다른 앱 진입 차단 + 화이트리스트 앱 허용 확인)
- [ ] 안드 위젯 강제 갱신 알람(B단계, AlarmReceiver) — 앱 강제종료 상태에서 타이머 종료 시각에
      위젯 '집중 중' 해제되는지 (빌드 46+ 코드, 검증 기록 없음)
- [ ] 새 온보딩 5단계 (b3b7b61) — 신규 설치 플로우 1회 확인

## 4. OTA 배포분 실기기 확인 (빌드 무관 — 폰이 업데이트 받은 뒤)

- [ ] 카운트업 5시간 상한: 자유 타이머 방치 → 5:00:00 자동 종료+세션 기록
- [ ] 스터디룸: 방 생성→퇴장 시 빈 방 삭제, 라운지 입장/자리선택/모드 표시
- [ ] 통계 주간 탭: 일~토 달력 주 표시, 미래 요일 흐림, 지난주 비교
- [ ] D-Day 삭제 시 시험 할일 동반 삭제 + 이미 있던 유령 오늘할일이 재시작 후 사라지는지
- [ ] 월간 탭: 29~31일에 이전/다음 달 이동 정상 (다음 29일 이후에나 확인 가능)

## 4.5 ★시간 입력 개편 (2026-08-02) — **안드·iOS 실기기 전항목 통과**★

**배경**: 김진 제보 — 고정 일정 시간을 고칠 때마다 드럼롤을 굴려야 해서 느리다.
사용자도 타이머 설정에서 같은 불편을 느끼고 있었음.

**최종 형태**: 휠(TimePickerGrid)과 스테퍼(Stepper)를 **폐기**하고 네모 입력창으로 통일.
1차 시도(36d3ca2)는 휠 위에 '숫자 탭 → 타이핑'을 얹었으나 부가 기능처럼 보여 어색하다는
판단으로 갈아엎음(ad40a9e). 빠른 선택 칩(5·25·60분 등)은 유지 — 한 번 탭으로 끝나
답답함의 원인이 아니었다.

- `TimeField`(신설): 시각 입력 = 네모 [시] + 네모 [분]. 분이 5분 단위에 묶이지 않음
- `NumberField`(신설): 수치 입력 = 네모 칸 하나. Stepper·EditableNumber 대체
- 적용 10곳: 플래너 계획/고정일정, 일정편집 고정일정·계획, 설정 리마인더,
  카운트다운 시간, 뽀모 집중·휴식, 결과모달 시·분, 연속모드 항목별 분
- **미적용(의도)**: 플래너 목표량 / 일정편집 목표 시간 / 설정 일일 목표 — 프리셋 칩만 있음

**설계 규칙 (되돌리지 말 것)**
- 커밋은 **글자마다 라이브**. 확인 버튼이나 blur를 기다리면 "타이핑 직후 저장"에서 값이
  유실된다(버튼 press와 blur의 순서가 플랫폼마다 다름). min/빈칸 보정만 blur에서
- **컴포넌트가 친 값을 말없이 바꾸지 않는다** — minValue 하한을 넣었다가 되돌렸다(be9b6ed).
  고정 일정은 자정 넘김(23:00~07:00)이 **허용된 기능**이라 클램프가 정상 입력을 막았고,
  07을 쳤는데 23이 되는 건 사용자에겐 버그로 읽힌다. 관계 검증은 저장 시점 화면의 몫
- `TimeField` 루트에 `flex:1`을 박지 말 것 — 행에서는 반씩 나눠 갖는 뜻이지만 세로
  컨테이너에 홀로 놓이면 flexBasis 0이 되어 **높이가 0으로 접힌다**(2c16a9e)

### ★이번에 밟은 함정: 안드 Modal은 별도 창이라 'pan'이 안 먹는다★

`app.config`의 `softwareKeyboardLayoutMode: 'pan'`은 **메인 액티비티 창에만** 적용된다.
RN `Modal`은 별도 Dialog 창이라 키보드가 올라와도 밀리지 않아 입력칸이 그대로 묻힌다.
휠 시절엔 키보드를 안 써서 드러나지 않다가 타이핑으로 바꾸며 표면화(1d904d0).
→ 시간 입력이 있는 모달 4곳에 `KeyboardAvoidingView(behavior="padding")`.
스터디룸이 이미 같은 함정을 같은 방법으로 풀어두고 주석까지 남겨둔 상태였다.

곁들여 드러난 것:
- 일정편집 '과목' 모달은 **세로 ScrollView가 아예 없었다** — 시트 92% 고정이라 넘치는
  내용(목표 시간·시간 입력)이 잘려 접근 불가. 이번 개편 전부터 있던 문제
- 설정 리마인더 시트는 `position:'absolute'`라 KAV의 padding이 밀어내지 못한다 → flex-end 배치
- '특정 시간에 배치' 토글을 켜면 새로 생긴 시간 줄이 잘린 채 나옴 → 켤 때 그 줄까지 스크롤
  (카드 탭·스위치 탭 **두 경로 모두** 필요, 57c8222)

### 자정 넘김 통일 (a5b6f77)

그리드는 이미 자정 넘김을 완전히 지원하고 있었다(blockGeometry가 당일 24:00까지,
carryoverGeometry가 다음 날 이어지는 부분, freeSlots·occupiedIntervalsForDay도 감안).
그런데 **플래너탭 BlockModal만 end <= start를 일괄 거부**해 취침 23:00~07:00을
일정편집에서만 만들 수 있었다 → 고정 일정은 허용 + 확인창, 공부 계획은 계속 금지
(목표량이 '종료 - 시작'이라 음수). `spanMinutes` 순수 함수로 중복 공식 통합.

### 실기기 확인 (안드 2026-08-02 — **전항목 통과**)

- [x] 시 09 / 분 37 저장 → 5분 단위가 아닌 값 정상, 블록 배치도 정상
- [x] 타이핑 직후 확인 없이 바로 저장 → 값 유지
- [x] 빠른 선택 칩과 입력창 연동
- [x] 종료 칸에 시작보다 이른 값 → **튀지 않음**
- [x] 키보드 가림 4곳(플래너 일정, 일정편집 고정일정, 커스텀 타이머, 리마인더)
- [x] 과목 수정 모달 스크롤 / 리마인더 시트 레이아웃 / 시작·종료 좌우 반씩
- [x] 결과모달 시·분 두 칸 세로 정렬
- [x] 연속모드 항목 분 입력(쉬는시간 초록 유지) · 뽀모 집중/휴식
- [x] 자정 넘김: 확인창 → 저장 → 그리드 캐리 표시 → 공부 계획은 차단 → 가용시간 반영

### ★iOS 검증 완료 (2026-08-03, build57 TestFlight) — 위 목록 전항목 통과★

이번 개편에서 나온 회귀 3건(키보드 가림 / minValue 클램프 / flex:1 붕괴)이 **전부
코드로는 안 드러나고 실기기에서만 보이는 종류**였다. iOS는 KAV 동작과 키보드 높이가
안드와 달라 같은 확인이 필요하다.

## 4.6 ★스터디룸 2건 (2026-08-02, 25d0d58) — **안드·iOS 전항목 통과**★

iOS 실기기 제보 2건(응원 받기 토스트 없음 / 아이폰 타이머 종료가 갤럭시에 안 보임)의
**뿌리가 하나**였다.

1. **`studyRoomEnabled`가 꺼진 채 방에 있는 상태** — 이 플래그는 create/join/joinLounge
   에서만 켜졌다. 익명 uid는 iOS 키체인으로 재설치에도 살아남으므로(durableAuthStorage)
   앱을 다시 깔면 **방 멤버십은 서버에 남고 로컬 설정만 초기화**된다. 이 상태에선 방 화면은
   자기 구독으로 멀쩡히 돌지만 useAppState의 스터디룸 작업이 전부 꺼진다 —
   presence 동기화·하트비트·받은 응원 토스트·'우리 방 N명' pill.
   → 서버 멤버십이 확인되면 플래그를 켠다(자가 치유).
   ※**증상 구분법: 남이 공부 중인데 집중탭에 pill이 안 뜨면 플래그가 꺼진 것**
2. **`displayStatus`가 plannedEndAt을 상한으로 쓰지 않았다** — 신뢰창을 '늘리는' 조건으로만
   써서, 50분 카운트다운이 끝나도 updatedAt이 60분 신뢰창 안이면 친구 화면에 최대 60분간
   '공부 중'이 남았다. iOS는 백그라운드에서 JS가 멈춰 종료를 알릴 수 없으므로(구조적 한계)
   보는 쪽이 스스로 판단해야 한다. → 예정 종료 + 5분이 지나면 내려간다.
   - [x] 갤럭시에서 확인 완료 (아이폰 카운트다운 종료 후 5분 뒤 해제)
   - 남는 한계: **누적 시간(오늘 N분)은 아이폰이 앱을 열 때까지 갱신되지 않는다.**
     남의 공부 시간을 추측해 미리 더하는 건 과대 집계 위험이라 손대지 않음


## 4.7 ★오답노트 첨부가 안드 자동 백업을 깨뜨리던 문제 (2026-08-02) — **네이티브 빌드 필수(OTA 불가)**★

**발견**: "사진·오디오를 많이 쓰면 어떻게 되나"라는 질문에서 상한을 세어보다 드러남.
동영상 얘기가 아니라 **이미 배포된 사진 첨부(1.0.38~)만으로 걸리는 문제**였다.

- 노트당 5장 상한은 있지만 **노트 개수·총 용량 상한이 없다**
- 첨부는 `documentDirectory`(= `files/`)에 쌓이고, 이 폴더는 안드 자동 백업 대상
- 안드 자동 백업은 **앱당 25MB 상한**, 넘으면 백업이 통째로 실패
- 1600px·q0.6 압축 사진이 장당 200~400KB → **사진 꽉 채운 노트 ~17개면 25MB**
- 백업이 실패하면 `allowBackup: true`로 지키려던 것이 무너진다 —
  재설치 시 **익명 uid(스터디룸 계정)와 공부 데이터 복원이 조용히 안 됨**
  (계정 영속 1단계, 2026-07-19 승인). 사용자는 원인을 알 방법이 없다
- 확인: 이 프로젝트에는 백업 제외 규칙이 **아예 없었다**(매니페스트에 `allowBackup="true"`만)

**해결**: `plugins/withAttachmentsNotBackedUp.js` — 첨부 폴더만 클라우드 백업에서 제외.
`res/xml/data_extraction_rules.xml`(API 31+)와 `backup_rules.xml`(API 30-)을 생성하고
매니페스트 `<application>`에 연결한다. AsyncStorage(uid·공부 데이터) 백업은 그대로.
기기 간 직접 전송(device-transfer)은 용량 제한이 없어 **제외하지 않는다**(새 폰엔 사진도 따라감).

- 잃는 것 없음: JSON 백업/복원(`storage.js` BACKUP_KEYS)도 노트 기록만 담고 이미지 파일은
  애초에 안 옮긴다. 자동 백업이 사진을 옮기던 유일한 경로였고 그마저 25MB에서 깨졌다
- prebuild로 생성 확인 완료 (매니페스트 속성 2개 + res/xml 파일 2개)

**확인 항목 (다음 네이티브 빌드에서)**
- [ ] `adb shell dumpsys backup` 또는 재설치 복원으로 uid·공부 데이터가 살아오는지
      (사진 첨부를 25MB 넘게 만든 상태에서 — 예전엔 여기서 백업 자체가 실패했다)
- [ ] 재설치 후 오답노트 **기록은 살아 있고 사진만 비는지** (의도된 동작)
- [ ] Play 검토 화면 '지원 기기 변경사항' 이상 없는지 (권한 추가는 아니지만 습관적으로 확인)

★**설정 문구는 이 빌드와 함께 나가야 한다**★ — '백업/복원에는 포함되지 않아요'는
플러그인이 실린 빌드에서만 사실이다. 구 런타임(1.0.38/1.0.39)에 이 문구만 OTA로 보내면
거짓말이 된다(거기선 여전히 백업에 들어가고 25MB에서 깨진다).

**후속(미구현)**
- iOS iCloud 백업 제외(`NSURLIsExcludedFromBackupKey`) — expo-file-system에 API가 없어
  네이티브 작업 필요. iOS는 uid가 키체인에 있어 계정 영속은 안전하고, 남는 문제는
  사용자 iCloud 용량 소모뿐이라 후순위
- 오디오 첨부: expo-audio가 이미 있어 새 의존성 0개. `RECORD_AUDIO` 권한이
  `android.hardware.microphone`을 암시하므로 **withCameraNotRequired와 같은 패턴의
  플러그인이 반드시 필요**(규칙 12). iOS는 녹음 시 오디오 세션 카테고리가 바뀌므로
  재생 중인 백색소음을 멈췄다 되돌려야 한다
- 동영상 첨부: 권하지 않음. 압축 API가 없고, `videoQuality`는 **iOS 전용**,
  `videoMaxDuration`은 안드에서 카메라 앱이 무시할 수 있다(설치된 패키지 타입 정의로 확인).
  앨범 선택은 `fileSize`/`duration`으로 사전 거절이 가능해 오히려 통제하기 쉽다

## 4.8 ★사진까지 담는 백업(zip) (2026-08-02) — **안드·iOS 전항목 통과**. 네이티브 빌드 필수(OTA 불가)★

**왜**: 4.7에서 첨부를 안드 자동 백업에서 제외하면서, 사진이 새 폰으로 옮겨가는 경로를
정리해보니 **온전한 길이 하나도 없었다**.

| 경로 | 사진이 따라가나 |
|---|---|
| 앱의 JSON 백업 | ✗ 노트 기록만, 이미지 파일은 안 담김 |
| 아이폰 → 아이폰 (iCloud/빠른 시작) | ○ (iOS는 제외 안 함) |
| 안드 → 안드 (기기 간 직접 전송) | ○ (device-transfer는 일부러 안 뺌) |
| 안드 → 안드 (클라우드 백업만) | ✗ 4.7 이후 |
| **안드 ↔ 아이폰 (기종 변경)** | **✗ 어떤 경로로도 안 감** |

**해결**: 백업 파일 하나(zip) 안에 `backup.json` + `attachments/`를 함께 담는다.
기존 흐름(내보내기 → 카톡·드라이브 저장 → 새 폰에서 파일 선택)이 그대로 유지되고
기종 변경까지 해결된다. `src/utils/backupArchive.js` (순수 로직 테스트 11개).

- ★**성립 근거: 노트는 첨부의 파일명만 저장한다**★(attachments.js) — 같은 이름으로
  새 폰의 앱 폴더에 되돌려 놓으면 링크가 그대로 살아난다. 경로를 저장했다면 불가능했다.
  **이 설계를 바꾸지 말 것**
- 압축은 `react-native-zip-archive@9`(New Arch **TurboModule**, 2026-07 유지보수 중).
  파일 경로로 직접 압축하므로 메모리 안전 — 순수 JS zip(jszip/fflate)은 파일을 base64로
  메모리에 올려야 해서 **정작 사진이 많은 사용자에게서 터진다**
- 내보낼 때 사진 포함 여부를 묻는다(장수·용량 표시). '기록만'은 예전과 똑같은 JSON 한 장
- 복원은 **매직바이트로 판별**(`UEsDB`) — 카톡·드라이브를 거치며 이름이 바뀌어도 맞고,
  **옛 .json 백업도 그대로 복원된다**(하위호환)
- 되돌리는 파일은 **복원할 노트가 참조하는 것만** — 손상·조작된 아카이브가 엉뚱한 파일을
  앱 폴더에 심는 것 방지(`filesToRestore`)
- zip이 폴더 이름을 엔트리 경로에 포함하는지는 플랫폼/버전마다 다를 수 있어,
  압축 해제 후 `backup.json`을 루트와 한 겹 안쪽에서 찾는다(`findRoot`) — 의존하지 않는다

**확인 항목 (다음 네이티브 빌드에서)**
- [x] 사진 있는 상태에서 '데이터 백업' → '사진도 함께' → zip이 만들어지고 공유되는지
- [x] 그 zip을 **다른 기기**에서 복원 → 노트와 **사진 썸네일까지** 살아나는지
- [x] '기록만' 백업 → .json → 복원 정상 (예전과 동일)
- [x] ★**예전에 만들어둔 .json 백업 파일도 복원되는지**★ (하위호환 — 실사용자가 갖고 있다)
- [x] 사진 100장 규모에서 메모리·시간 (압축 진행 중 앱이 죽지 않는지)
- [x] 복원 취소 시 임시 폴더가 정리되는지

## 4.9 ★오답노트 음성 메모 (2026-08-03) — **안드·iOS 전항목 통과**. 네이티브 빌드 필수★

**무엇**: 오답노트에 음성을 남긴다. 직접 녹음하거나(하나당 3분) 가지고 있는 오디오 파일을 붙인다.
노트당 2개(녹음·첨부가 같은 칸을 나눠 쓴다 — 사용자에겐 '음성 N개' 하나의 개념).

**상한을 이렇게 정한 이유**
- 길이 3분: "왜 틀렸는지" 설명은 대개 30초~1분. 상한은 예산이 아니라 **잊고 켜둔 녹음** 방지용
- 개수 2개: 복습 루프가 있어 '처음 설명' + '복습 때 다시 설명'이 자연스럽다
- 노트당 최대 약 3MB(사진 5장 1.5MB와 별도). 상수 두 개(`MAX_AUDIO`, `MAX_AUDIO_MS`)라 조정은 한 줄

### ★플랫폼 재생 호환이 이 기능의 핵심 제약★

백업 zip으로 안드↔아이폰 기종 변경을 커버(4.8절)했기 때문에, **안드에서만 재생되는 포맷이
들어오면 그 커버가 깨진다**(아이폰에서 복원해도 음성만 안 들림). 두 곳에서 막았다.

1. **녹음**: `expo-audio`의 `RecordingPresets`를 **쓰지 않는다**. `LOW_QUALITY`가 안드에서
   `.3gp` + AMR-NB로 녹음하는데 iOS가 AMR 재생을 지원하지 않는다.
   → 양 플랫폼 m4a(AAC) 모노 64kbps로 직접 지정(분당 약 0.5MB). **테스트가 이 조건을 지킨다**
2. **파일 첨부**: 확장자 화이트리스트 `m4a·mp3·aac·wav`. 안드는 ogg·flac·amr을 재생하지만
   iOS는 못 하므로 첨부 단계에서 거절(이유를 사용자에게 말해준다)
   - 용량 상한 **10MB** — 녹음은 3분으로 묶이지만 남의 파일은 몇십 분짜리도 들어온다.
     백업 zip에 그대로 담기므로 여기서 안 막으면 백업이 못 쓰게 커진다

### 이번에 드러난 결함 2건 (음성이 없었으면 안 보였을 자리)

- ★**편집기가 첨부의 필드를 흘리고 있었다**★ — 노트를 열고 저장할 때 `{ file }`만 뽑아
  배열을 다시 만들어서 음성의 `type`·`durationMs`가 사라지고 **사진으로 변했다**
  (썸네일 자리에 빈 칸, 재생 불가). `normalizeAttachments`로 한 곳에서 정하고 테스트로 고정
- ★**목록 카드 썸네일이 '첫 첨부'를 Image로 그렸다**★ — 음성이 첫 번째면 빈 칸.
  첫 '사진'을 쓰고, 사진 없이 음성만 있으면 마이크 아이콘. 배지는 사진+음성 합계

### 그 밖의 설계 결정

- **녹음 중 집중 사운드 정지 → 끝나면 복원**(`useAppState.suspendSounds/resumeSounds`).
  iOS는 녹음 시작 시 오디오 세션 카테고리가 바뀌어 재생이 죽거나 백색소음이 녹음에 섞인다.
  ★`settings.activeSounds`는 건드리지 않는다★ — 지우면 저장까지 되어 사용자의 선택이 영구 손실.
  종료 시 `setAudioModeAsync(allowsRecording:false)`를 **먼저** 되돌린 뒤 재개(순서 바뀌면 무음)
- **진행바 + 탭해서 구간 이동**: 줄마다 플레이어를 따로 둔다(`AudioMemoRow`). 부모가 imperative
  플레이어 하나로 돌리면 진행 상태를 못 그린다. 동시 재생 방지는 부모의 `playingFile`이 주도
- 3분 도달 시 자동 정지(사용자 정지와 겹쳐 두 번 저장되는 것은 `stoppingRef`로 방지),
  1초 미만은 저장하지 않음(실수 탭)
- `RECORD_AUDIO` 권한 + `plugins/withMicrophoneNotRequired.js`
  ★규칙 12★ — CAMERA 때 기기 402대가 빠진 것과 같은 계열. prebuild로 매니페스트에
  `android.hardware.microphone required="false"` 반영 확인

### 실기기 확인 (안드 2026-08-03 — **전항목 통과**)

- [x] 녹음 → 저장 → 노트 다시 열기 → 재생 (첨부 필드 유실 결함 자리)
- [x] 백색소음 켜둔 채 녹음 → 녹음 중 정지, 종료 후 복원, 녹음본에 안 섞임
- [x] 3분 자동 정지 / 1초 미만 무시
- [x] 마이크 권한 거부 안내 / 2개 채우면 버튼 사라짐
- [x] 백업 '사진·녹음' → 복원 → 음성까지 재생
- [x] 진행바 탭 → 구간 이동, 시간 표시 정확
- [x] 끝까지 듣고 다시 누르면 처음부터
- [x] 2개 중 한쪽 재생 시 다른 쪽 정지
- [x] mp3 첨부 재생 / .ogg·10MB 초과 거절 메시지 / 첨부 파일 길이 표시
- [x] 목록 카드 마이크 아이콘 (사진 있으면 사진 우선)

### iOS 확인 (2026-08-03 build57 TestFlight — **전항목 통과**)

- [x] 녹음 중 백색소음 정지·복원 (**세션 카테고리가 안드와 가장 다른 영역**)
- [x] 첨부한 mp3·wav 재생 (화이트리스트를 건 이유가 여기)
- [x] 진행바 seekTo 동작
- [x] 마이크 권한 문구(NSMicrophoneUsageDescription) 노출


## 4.10 ★★현재 sdk56 HEAD를 1.0.39 런타임에 OTA로 내보내지 말 것★★ (2026-08-03)

08-02~03 작업 중 시간 입력 개편·스터디룸·플래너는 원래 OTA 가능한 JS 변경이었다.
**그런데 같은 브랜치에 네이티브가 필요한 기능이 얹히면서 번들이 통째로 1.0.40 전용이 됐다.**
JS 번들은 부분 배포가 안 되므로 셋만 골라 보낼 수 없다.

1.0.39 런타임에 이 번들을 보내면:
- ★**iOS 크래시**★ — 음성 녹음 버튼 → 마이크 권한 요청 → 1.0.39에는
  `NSMicrophoneUsageDescription`이 없다. iOS는 usage description 없이 권한을 요청하면
  **앱을 즉시 종료**시킨다(경고가 아니라 크래시)
- 안드: `RECORD_AUDIO` 미선언이라 녹음이 그냥 실패
- 백업에서 '사진·녹음'을 고르면 `react-native-zip-archive`가 없어 실패
  (lazy require라 앱 로드는 되지만 그 동작에서 죽는다)
- 설정·오답노트 문구가 없는 기능을 설명한다(백업에 사진이 담긴다, 음성 메모가 있다)

→ **1.0.40은 스토어 심사로만 나간다.** 세 기능을 1.0.39 사용자에게 먼저 주고 싶다면
그것만 담은 별도 브랜치를 만들어야 하는데, 심사 기간을 감안하면 값어치가 낮다(하지 않기로).

※다음에 같은 상황을 피하려면: **OTA로 낼 것과 네이티브가 필요한 것을 같은 브랜치에 섞지 말 것.**
섞는 순간 OTA 가능분도 함께 묶인다.

## 5. 빌드/제출 절차 (요약 — 상세는 CLAUDE.md)

```bash
eas build --profile testflight --platform ios      # iOS (사용자 승인 후)
eas build --profile production --platform android  # AAB (사용자 승인 후)
```
- EAS 무료 크레딧은 매월 1일 초기화 — 부족 안내가 떠도 월초 기준으로 판단
- 제출 후: App Store Connect / Play Console 심사 제출, 스크린샷은 `screenshots/store_v2/`
- ※eas update(OTA)는 `--non-interactive` 시 `--environment production` 필수,
  1.0.34 런타임 병행 발행은 version 임시 강하→복원 방식 (메모리 07-18 참조)

## 6.5 ★다음 빌드 릴리스 노트 수정 (필수) — 과대약속 제거

**문제**: 1.0.36 릴리스 노트("이 버전에서 업그레이드된 사항")에 아래 블록이 실려 나감(이미 배포됨,
소급 수정 불가). 안드로이드 자동백업은 조건부(24h·Wi-Fi·충전·구글계정)라 "재설치해도 유지"를
**단정하면 안 됨** — 작동 안 하면 사용자가 데이터 잃고 배신감만. (설명(Description) 필드엔 없음, 릴리스 노트 한정)

```
■ 재설치해도 안전하게
앱을 지웠다 다시 설치해도 공부 기록과 설정이 유지되도록 개선했어요.
```

**조치**: iOS·Android **둘 다 1.0.37 릴리스 노트에서 삭제**(2026-07-23 결정). 소급 수정은
불가 확인됨 — iOS 1.0.36은 배포 준비됨 상태라 잠김, Android vc64(1.0.37)도 심사 제출본이라 편집 불가.
따라서 **다음에 노트를 새로 쓰는 시점(iOS 1.0.37 빌드 제출 / Android 그 다음 릴리스)** 에서 아래 블록을 뺀다.
남기고 싶으면 자동유지 약속 대신 수동 백업 유도로:
```
■ 백업이 더 안심돼요
설정 탭에서 공부 기록을 파일로 백업·복원할 수 있어요. 기기를 바꾸거나 앱을 다시 설치하기 전에 백업해 두세요.
```
- 참고: 웹사이트 FAQ(index.html)는 이미 정직함("기기 교체·재설치 전 백업 권장") → 이 톤으로 통일.
- allowBackup:true 자체는 유지(조용한 best-effort 보너스, 콜드스타트 복원 방어 있어 안전) — 문구만 문제.

## 6.5-A ★1.0.39 릴리스 노트 (2026-08-01 작성) — **스토어별로 다르다**

★**하나의 노트를 두 스토어에 그대로 쓰면 안 된다**★ — Android는 직전이 1.0.38(vc68)이고
iOS는 직전이 **1.0.36**이라 "새로운 것"의 범위가 다르다. 실제로 08-01에 1.0.38 시절 노트를
그대로 쓸 뻔했다: 상시 알림·오답노트 사진 첨부·스터디룸 개편·결과 화면은 **Android엔 이미
1.0.38로 나갔고**, 그걸 다시 새 기능이라고 안내하는 꼴이었다.
(반대로 그 목록엔 1.0.39의 핵심인 공부 모드·이탈 판정 변경이 **한 줄도 없었다**.)

### Android (1.0.38 → 1.0.39)
```
■ 공부 모드 선택
시작할 때 일반·집중·울트라 중에 고를 수 있어요. 매번 묻지 않게 설정할 수도 있어요.

■ 화면을 꺼도 집중이 끊기지 않아요
화면을 끄거나 기기를 잠가도 이탈로 잡히지 않아요. 전화를 받거나 끊은 직후도 마찬가지예요.

■ 배터리 부담 감소
집중모드가 화면을 계속 켜두지 않아요. 기기의 화면 시간 초과 설정을 그대로 따라요.

■ 그 밖에
이탈 알림 정확도 개선, 오답노트 편집 키보드 개선.
```

### iOS (1.0.36 → 1.0.39) — 위 4항목 + 그동안 Android에만 있던 것 따라잡기
```
■ 공부 모드 선택
시작할 때 일반·집중·울트라 중에 고를 수 있어요. 매번 묻지 않게 설정할 수도 있어요.

■ 화면을 꺼도 집중이 끊기지 않아요
화면을 끄거나 기기를 잠가도 이탈로 잡히지 않아요. 전화를 받거나 끊은 직후도 마찬가지예요.

■ 배터리 부담 감소
집중모드가 화면을 계속 켜두지 않아요. 기기의 화면 시간 초과 설정을 그대로 따라요.

■ 오답노트 사진 첨부
틀린 문제를 촬영하거나 앨범에서 골라 붙일 수 있어요(최대 5장). 사진은 기기에만 저장돼요.

■ 같이 공부하기 개편
화면을 꺼도 '화면끔 몰입'으로 표시, 집중 중에도 우리 방 인원 확인, 다같이 집중(공유 카운트다운),
익명 응원, 방 오늘 공부 목록, 숨기기·신고 추가.

■ 그 밖에
완료 결과 화면이 어느 탭에서든 뜨도록 수정, 이탈 알림 정확도 개선,
오답노트 편집 키보드·위젯 표시 개선.
```

- ※**iOS 노트에서 '잠금화면·알림바에 공부시간'은 뺐다** — Live Activity는 1.0.36 이전부터
  있던 기능이라 iOS 사용자에겐 새것이 아니다(알림바 상시 알림은 Android 전용)
- ※6.5절의 "재설치해도 유지" 블록은 여기에 **없다**(그 숙제는 소화됨)
- ※'방 오늘 공부 목록'은 실제로 구현돼 있다(`StudyRoomScreen` 로스터, 오늘 공부시간 순 정렬).
  07-24 메모의 'E 방오늘공부리스트 미구현'은 낡은 기록

## 6.5-A2 ★1.0.40 릴리스 노트 (2026-08-03 작성) — **1.0.39 리젝으로 iOS 범위가 다시 넓어졌다**

**왜 다시 쓰나**: iOS 1.0.39(build56)가 CallKit/중국으로 리젝돼(0-AAAAA절) **1.0.39를 건너뛰고
build57(1.0.40)로 직행**하기로 했다(2026-08-03 사용자 결정). 그래서 iOS 노트의 범위가
`1.0.36 → 1.0.40`이 된다 — 위 6.5-A절의 iOS 초안(1.0.36→1.0.39)에 08-02~03 작업분을 얹은 것이다.
★**6.5-A절 iOS 초안을 그대로 쓰면 음성 메모·백업·시간 입력이 통째로 빠진다**★

Android는 vc70(1.0.39)이 **승인·배포됐다면** 범위가 `1.0.39 → 1.0.40`으로 좁다.
※vc70이 아직 심사 중이거나 반려됐다면 Android도 범위를 다시 잡아야 한다 — **제출 전 Play 상태 확인**.

### iOS (1.0.36 → 1.0.40)
```
■ 공부 모드 선택
시작할 때 일반·집중·울트라 중에 고를 수 있어요. 매번 묻지 않게 설정할 수도 있어요.

■ 화면을 꺼도 집중이 끊기지 않아요
화면을 끄거나 기기를 잠가도 이탈로 잡히지 않아요. 전화를 받거나 끊은 직후도 마찬가지예요.

■ 배터리 부담 감소
집중모드가 화면을 계속 켜두지 않아요. 기기의 화면 시간 초과 설정을 그대로 따라요.

■ 오답노트에 사진과 음성
틀린 문제를 촬영하거나 앨범에서 골라 붙일 수 있어요(최대 5장). 왜 틀렸는지 음성으로도
남길 수 있어요(노트당 2개). 사진과 녹음은 기기에만 저장돼요.

■ 사진·음성까지 담는 백업
설정 탭에서 백업할 때 사진과 녹음을 함께 담을 수 있어요. 기기를 바꿔도 그대로 옮겨가요.

■ 시간 입력이 편해졌어요
계획과 타이머 시간을 휠 대신 숫자로 바로 입력해요. 5분 단위에 묶이지 않아요.

■ 같이 공부하기 개편
화면을 꺼도 '화면끔 몰입'으로 표시, 집중 중에도 우리 방 인원 확인, 다같이 집중(공유 카운트다운),
익명 응원, 방 오늘 공부 목록, 숨기기·신고 추가.

■ 그 밖에
완료 결과 화면이 어느 탭에서든 뜨도록 수정, 이탈 알림 정확도 개선,
오답노트 편집 키보드·위젯 표시 개선.
```

### Android (1.0.39 → 1.0.40) — vc70 승인 전제
```
■ 오답노트 음성 메모
왜 틀렸는지 음성으로 남길 수 있어요. 직접 녹음하거나 가지고 있는 오디오 파일을 붙여요(노트당 2개).

■ 사진·음성까지 담는 백업
설정 탭에서 백업할 때 사진과 녹음을 함께 담을 수 있어요. 기기를 바꿔도 그대로 옮겨가요.

■ 시간 입력이 편해졌어요
계획과 타이머 시간을 휠 대신 숫자로 바로 입력해요. 5분 단위에 묶이지 않아요.

■ 그 밖에
취침처럼 자정을 넘기는 고정 일정을 플래너에서도 추가할 수 있어요.
같이 공부하기 표시 정확도와 백업 안정성을 개선했어요.
```

- ※**안드 자동 백업 제외(4.7절)는 노트에 안 쓴다** — 사용자 눈엔 "사진이 백업에서 빠졌다"로만
  읽혀 손해처럼 보인다. 실제 해결책인 '백업에 사진 담기'는 위에 있고, 그쪽이 사용자가 쓰는 기능이다
- ※**iOS 노트에 '백업 안정성'을 넣지 않았다** — 4.7절은 Android 전용 문제다
- ※6.5절의 "재설치해도 유지" 과대약속 블록은 여기에도 **없다**(계속 지킬 것)
- ※마이크 권한이 처음 들어가는 빌드다. **App Privacy(개인정보 보호) 답변에 오디오 항목이
  필요한지 ASC에서 확인할 것** — 녹음은 기기 밖으로 나가지 않으므로 '수집 안 함'이 맞지만,
  사진 첨부 때와 마찬가지로 질문 자체는 뜬다

## 6.5-B ★스토어 긴 설명(Description) 갱신 (2026-08-01)

- [x] **Play 등록정보 설명 갱신 — 2026-08-01 사용자 작업 중** (Android는 1.0.38로 전 기능이
      이미 라이브라 언제 올려도 정확하다)
- [ ] **App Store 설명 갱신은 1.0.39 빌드와 _함께_ 제출할 것** — 스터디룸 후속(익명 응원·
      다같이 집중·숨기기·신고)과 오답노트 사진 첨부는 **iOS에선 1.0.39가 승인돼야 실제로 있다**.
      먼저 올리면 없는 기능을 광고하게 된다(6.6절 웹사이트와 같은 이유)

### ★이번에 드러난 것: UI 문구를 바꾸면 스토어 설명이 조용히 낡는다★
07-30~31에 모드 이름을 **일반/집중/울트라모드**로 통일했는데, 스토어 설명은 **옛 이름
(집중 도전 / 울트라집중 / 편하게 공부)** 그대로였다. 앱 어디에도 없는 단어로 기능을 설명하고
있었던 셈이다. 내용도 어긋나 있었다:
- "집중 도전 = **화면을 켜둔 채** 공부" → 1.0.39에서 keep-awake 제거(§1.7)
- "울트라집중 = 챌린지 문구" → 챌린지는 **집중모드**(1분 이탈) 특징. 울트라는 **10초 정지 +
  일시정지 불가 + (안드) 화면 고정**
- "편하게 공부 = 화면 꺼도 OK" → 1.0.39부터 화면 끄기는 **모든 모드**에서 이탈이 아니다

### 교체한 3번 항목 (확정본)
```
3. 공부 모드 — 시작할 때 골라요

타이머를 시작할 때마다 세 가지 중에 고르고, 고른 모드만큼 집중밀도 점수에 보너스를 받아요. 매번 묻지 않고 바로 시작하도록 설정할 수도 있어요.

일반모드 (+5): 이탈 감지 없음, 일시정지 자유. 부담 없이 시작하고 싶을 때 추천합니다.

집중모드 (+10): 폰을 내려놓고 공부. 1분 넘게 다른 앱을 쓰면 챌린지 문구를 따라 써야 돌아올 수 있어요. 이탈 0회로 완주하면 Verified 인증을 획득해요.

울트라모드 (+15): 가장 강력한 잠금. 일시정지가 막히고, 10초만 다른 앱을 써도 타이머가 멈춰요. 안드로이드는 화면까지 고정됩니다.

화면을 끄거나 기기를 잠가도, 전화를 받거나 끊은 직후에도 이탈로 잡히지 않아요. 집중모드가 화면을 계속 켜두지 않아 배터리 부담도 없습니다.
```
※보너스 숫자(+5/+10/+15)는 `App.js MODE_CHOICES.bonus`와 같아야 한다 — 앱이 팝업 카드에
  같은 숫자를 노출하므로 어긋나면 약속이 깨진다(`density.js` 공식 변경 시 3곳 함께)

### 그 밖에 고친 곳
- 2번 "다른 공부 타이머에는 없는 기능" → **"열공메이트의 핵심 기능"** (검증 불가 단정 제거)
- 6번 "수능 모드" → **"수능 시뮬레이션"** (과목 탭의 실제 명칭 — 이름이 달라 못 찾는다)
- 7번 스터디룸에 **화면끔 몰입 / 다같이 집중 / 익명 응원 / 방 오늘 공부 / 숨기기·신고** 추가
  (전부 구현돼 있는데 설명에 없었다)
- 9번 "홈 화면·잠금화면 위젯" → **"홈 화면 위젯 5종"** + 잠금화면은 iPhone 한정으로 명시
  (안드로이드엔 잠금화면 위젯이 없다)
- 10번 백색소음에 **최대 3개 동시 믹스** 추가
- 확인만 하고 **안 고친 것**(정확했음): 백색소음 10종 / 캐릭터 4종 설명 / 위젯 5종 /
  잔디 4개월 / 티어 SS~C / 오답노트 5장·기기 저장 / 플래너 전 항목
- ⚠**다음 릴리스 숙제**: 제출된 Play 설명 3번에 `울트라모드 ... 10초만 다른 앱을 써도`가
  남아 있다. 08-01 앱 문구 정정(숫자 삭제)보다 **제출이 먼저 나가서** 못 맞췄다.
  실제 하한은 안드 15초 / iOS 30초라 **어느 쪽과도 안 맞는다** → 다음 등록정보 수정 때
  `잠깐만 다른 앱을 써도 타이머가 멈춰요`로 고칠 것(앱 문구와 동일하게, 숫자 없이).
  App Store 설명은 아직 제출 전이므로 **거기엔 처음부터 숫자 없이 넣을 것**

## 6.5-C ★Play 'AI asset declaration' — 매번 뜨는 항목이므로 답을 고정 (2026-08-01)

Play 콘솔 **스토어 등록정보 > 저장 직전 '애셋 리뷰'** 단계에서 매번 묻는다.
범위는 **스토어에 올리는 이미지**(아이콘·스크린샷·그래픽 이미지·프로모션 영상)뿐이고,
앱 기능이나 앱 안의 콘텐츠와는 무관하다.

- **답: `Label assets as created or edited using AI`** — 곰 아이콘과 캐릭터 4종
  (토루·팽이·타코·토토루) 원화가 **AI 생성물**이다(사용자 확인 2026-08-01)
- 애셋별 라벨링 기준:
  | 애셋 | 라벨 |
  |------|------|
  | 앱 아이콘(곰+스톱워치) | **O** — 원본이 AI. System.Drawing 배경 키잉은 일반 편집이라 판단에 무관 |
  | 그래픽 이미지(피처 그래픽) | 곰·캐릭터가 들어갔으면 **O** |
  | 스크린샷 | 캡처 자체는 AI가 아니지만 **화면 안에 캐릭터가 보이는 컷은 O** |
- ※`screenshots/store_v2/play/01.png`에는 캐릭터가 없다(UI + 텍스트 + 폰 목업뿐)
- ★**'Don't label'은 안전한 기본값이 아니라 진술이다**★ — AI 제작물을 아니라고 신고하면
  정책 위반. 반대로 라벨링해도 **노출·순위·심사에 불이익은 없다**(표시 의무일 뿐).
  애매한 컷은 라벨링하는 쪽으로 판단할 것

## 6.6 ★1.0.39 양대 출시 후 웹사이트 반영 (2026-07-29 보류분)

웹사이트(`main` 브랜치 = GitHub Pages)는 07-29에 오답노트·잠금화면 타이머까지 반영했다.
**아래는 "Android에만 나가 있어서" 일부러 뺀 것들** — iOS 1.0.39가 승인돼 양 플랫폼이
같아지는 순간 넣는다. 사이트가 "앱 안의 화면은 두 플랫폼이 동일"이라고 말하고 있어서,
한쪽에만 있는 기능을 그냥 쓰면 iOS 사용자에게 거짓말이 된다.

- **스터디룸 후속** — 익명 응원(엄지척) / 다같이 집중(공유 카운트다운) / 방 리스트 /
  라운지 안전장치(숨기기·신고·닉네임 필터). 반영처: `features/stats.html` 스터디룸 `<ul>`,
  `index.html` 스터디룸 섹션 hint, `guide.html` 6번 문단
- **오답노트 사진 첨부의 `Android 먼저` 태그 제거** — `features/planner.html#review-notes`
  3번째 카드의 `<span class="tag aos">`와 그 아래 hint 문장

**같은 시점에 고쳐야 할 1.0.39 동작 변경** (지금 문구는 라이브 기준으론 맞지만 출시되면 틀려짐):

### ★2026-08-01 전수 조사 — 적용할 편집 확정본★
사이트는 **지금 라이브(안드 1.0.38 / iOS 1.0.36) 기준으로 정확하다.** 아래는 전부
1.0.39가 승인된 뒤에 적용할 것 — 미리 올리면 **없는 동작을 안내하게 된다**.
★특히 '화면을 꺼도 이탈이 아니다'를 먼저 올리면, 1.0.38 사용자가 그 말을 믿고 화면을 껐다가
실제로 이탈을 먹는다★ — 사용자가 문구를 믿고 행동해서 손해를 보는 유형이라 가장 위험하다.

**(A) Android 1.0.39 승인 시 — 동작·이름 (양 플랫폼 공통 내용)**
1. **모드 이름 통일** `집중 도전`/`울트라집중`/`편하게 공부` → **`집중모드`/`울트라모드`/`일반모드`**,
   `잠금 강도` → **`공부 모드`**. 해당 위치(전수):
   - `guide.html` 80(본문 3단계 나열), 82·83·84(`<strong>` 라벨), 94(스터디룸 '울트라집중을 걸었는지'),
     125·126(스크린샷 alt)
   - `index.html` 60(FAQ 구조화데이터), 395(FAQ 본문 — '잠금 강도(일반·집중·울트라집중)' 포함),
     236·339(스크린샷 alt)
   - `features/focus.html` 91(3단계 나열 + '울트라집중은 일시정지도 차단'), 105, 108
2. `guide.html` 84 **"10초만 벗어나도"** → 숫자 삭제(`잠깐만 벗어나도`).
   실제 하한이 안드 15초/iOS 30초로 갈려 **숫자를 쓰면 또 틀린다**(앱 문구도 08-01에 같은 이유로 숫자를 뺐다)
3. **"즉시 알림"** → 첫 알림은 지연 예약이다(안드 5초 / iOS 20초).
   위치: `index.html` 130, `features/focus.html` 94.
   같이 넣을 것: **"알림을 보고 바로 돌아오면 이탈로 세지 않는다"**(설계 의도 — 앱 가이드와 같은 문장)
4. **화면 끄기·기기 잠금은 이탈이 아님** 한 줄 추가 (잠금 설명 근처) + **전화 수신·통화 직후도 아님**
5. **모드 선택 3지선다** — `guide.html` 3번이 '설정에서 미리 고르는 것'처럼 읽힌다.
   **타이머를 시작할 때마다 고르는 흐름**으로 손질(+ '다음부터 묻지 않기'도 한 줄)

**(B) iOS 1.0.39 승인 시 — 플랫폼 동등해지는 것** (위 6.6 본문의 보류분)
- `features/planner.html` 157 `<span class="tag aos">Android 먼저</span>` 제거
- 스터디룸 후속(익명 응원·다같이 집중·방 리스트·라운지 안전장치) 추가

※ 오답노트 스크린샷이 `screenshots/`에 없어 텍스트 카드로만 만들었다. 실기기 캡처가 생기면
`features/planner.html`의 카드 그리드를 다른 섹션처럼 `.frow` + 폰샷 형태로 올릴 수 있다.

## 6. 다음 결정 대기 (사용자)

- 계정 영속 B단계(Apple/Google 로그인 링크) 착수 여부 — 1단계 배포 후 피드백 보고
- 즐겨찾기 타이머 과목탭 이전 (OTA 가능, 07-12부터 미결정)
- 스터디룸 라운지 세분화(공시생/수능 등) — 수요 생기면
